---
status: passed
phase: 00.5.1-production-deploy-wire-up
score: 7/7 must-haves verified
verified: [CFA-11, CFA-12, DEPLOY-07, DEPLOY-08, DEPLOY-09]
failed: []
human_verification: []
verified_at: 2026-04-18T20:10:00Z
---

# Phase 0.5.1: Production Deploy Wire-Up — Phase Verification Report

**Phase Goal (from ROADMAP.md):**
> Leave Railway production in a state where any push to `origin/master` auto-deploys a healthy app with migrations run, env vars present, and CF Access still enforcing. After this phase completes, P1+P2 can ship with a single `git push`.

**Verified:** 2026-04-18T20:10:00Z
**Status:** passed
**Re-verification:** No — initial verification

Note: This phase already contained `00.5.1-VERIFICATION.md` as the operator paste-back log for the 7 checkpoints executed during Plans 01-03. To avoid overwriting that artifact, this phase-level verifier output lives at `00.5.1-PHASE-VERIFICATION.md` (distinct filename).

---

## Goal Achievement

### Observable Truths (from ROADMAP.md Success Criteria)

| # | Truth (Success Criterion) | Status | Evidence |
|---|---------------------------|--------|----------|
| 1 | Railway project has 3 services (tunnel, app, postgres) | VERIFIED | `Postgres` service provisioned 2026-04-18, service_id `4e32c3b7-bbd2-4957-96bb-db8489248c2f`, Hobby tier confirmed via Settings → Plan paste-back (VERIFICATION.md Checkpoint 1). `tunnel` + `npr-dashboard-prototype` preserved from P0.5 topology. |
| 2 | App has `DATABASE_URL` reference to Postgres | VERIFIED | `railway variables --service npr-dashboard-prototype --kv` shows `DATABASE_URL=postgresql://postgres:[rotated]@postgres.railway.internal:5432/railway` (resolved, not literal `${{...}}`). Runtime-proven: prestart Drizzle client connected during deploy `540160a4`. Reference set via CLI with single-quoted value (UI stored as literal twice — documented tribal knowledge). |
| 3 | App has `NEXT_PUBLIC_APP_NAME/DOMAIN/BRAND_COLOR` env vars | VERIFIED | Checkpoint 3 paste-back: `NEXT_PUBLIC_APP_NAME=NPR Dashboard`, `NEXT_PUBLIC_APP_DOMAIN=portal.utstitle.com` (bare, Zod-regex-compliant), `NEXT_PUBLIC_APP_BRAND_COLOR=#0F172A` (canonical default pinned). All 3 exact-match against pinned canonical values. |
| 4 | Repo has prestart/postdeploy migration hook | VERIFIED | `package.json` line 8: `"prestart": "npm run db:migrate"`. `node -e` check returns `npm run db:migrate`. Committed as `41ecb85`. |
| 5 | Direct Railway origin returns 403/404 | VERIFIED (with documented semantic equivalence) | `curl -I https://npr-dashboard-prototype-production.up.railway.app` → HTTP 401 + `server: railway-edge` + 12-byte `Unauthorized` body. Plan spec listed 403/404/refused/timeout; actual 401-from-edge is semantically equivalent (app never sees request, no leaked content). Justification captured in VERIFICATION.md Checkpoint 6. |
| 6 | Avatar renders real Google identity | VERIFIED (core invariant) | Operator signed in as Mike; avatar renders `"M"` (NOT P0.5 fallback `"CD"`). CFA-12 invariant = "avatar reflects signed-in user" → PASS. Single-char rendering (vs `"MM"`) is a separate UI polish bug filed as follow-up todo (see Follow-Ups section). |
| 7 | Fresh `git push` auto-deploys with migrations + seed + no Zod errors | VERIFIED | Deploy `540160a4-296f-4337-8573-4593d9fbc673` @ `e6a3658` status=SUCCESS, build=100.33s. Deploy logs show: `> prestart → npm run db:migrate → drizzle-kit migrate → [✓] migrations applied successfully! → next start → ▲ Next.js 16.2.4 → ✓ Ready in 88ms`. No Zod env errors. Seed executed separately per prestart-only policy; 6 rows in `drizzle.__drizzle_migrations`. |

**Score:** 7/7 success criteria verified

---

### Required Artifacts (Three-Level + Data-Flow Verification)

| Artifact | Expected | Level 1 (Exists) | Level 2 (Substantive) | Level 3 (Wired) | Level 4 (Data Flows) | Status |
|----------|----------|------------------|-----------------------|------------------|----------------------|--------|
| `package.json` prestart | `"prestart": "npm run db:migrate"` | YES | YES (1 line, correctly placed) | YES (npm lifecycle auto-fires before `start`) | YES (fired during deploy `e6a3658`, applied 6 migrations, proven at runtime) | VERIFIED |
| `.planning/DEPLOY.md` | 6-section runbook with 3-layer rollback | YES | YES (6 `##` headings grep-confirmed) | YES (§4 xrefs INITIAL_SEED_VERIFICATION.md; §5 xrefs CFA-11/12) | N/A (documentation) | VERIFIED |
| `.planning/INITIAL_SEED_VERIFICATION.md` | Filled-in verification artifact with outcome=verified | YES | YES (metadata + row counts + diff + outcome all populated) | YES (xrefs DEPLOY.md, VERIFICATION.md, seed files) | YES (commit SHA `e6a3658`, row counts 8/25/4/10/11/2 all match source) | VERIFIED |
| `00.5.1-VERIFICATION.md` | 7 checkpoint sections | YES | YES (Checkpoints 1-7 present) | YES (each checkpoint xrefs plan task + requirement ID) | N/A (audit log) | VERIFIED |
| Railway `Postgres` service | Hobby tier, private network, provisioned | YES (operator paste-back) | YES (tier confirmed Hobby, not Pro) | YES (private host `postgres.railway.internal`, password rotated post-exposure) | YES (6 migration rows + 8 tracks + 25 stages visible via readback) | VERIFIED |
| Railway app env vars | DATABASE_URL ref + 3 NEXT_PUBLIC_APP_* | YES | YES (all 5 keys present, exact values) | YES (runtime-resolved at deploy boot; Next.js booted without Zod errors) | YES (Drizzle connected, Next.js rendered) | VERIFIED |

All artifacts pass Levels 1-3; runtime artifacts also pass Level 4 (data flowing).

---

### Key Link Verification (Wiring)

| From | To | Via | Status | Evidence |
|------|-----|-----|--------|----------|
| `package.json prestart` | `npm run db:migrate` | npm lifecycle hook | WIRED | Deploy logs show the chain fired in sequence: `> prestart → drizzle-kit migrate → [✓] migrations applied` |
| `npm run db:migrate` | `drizzle/migrations/meta/_journal.json` | drizzle-kit reads journal + diffs DB | WIRED | 6 rows in `drizzle.__drizzle_migrations` match the 6 journal entries 0000..0005 |
| `git push origin master` | Railway build + deploy | Railway GitHub integration | WIRED | Deploy `540160a4` at commit `e6a3658` reached SUCCESS after 2 prior lock-file-fix commits (`0358eb0`, `0c4d295`, `e6a3658`) |
| Railway Postgres | `npr-dashboard-prototype.DATABASE_URL` | Reference variable `${{Postgres.DATABASE_URL}}` | WIRED | CLI resolves to real `postgresql://` connection string (not literal); runtime-proven during prestart Drizzle connection |
| Railway env vars (NEXT_PUBLIC_APP_*) | `lib/env.ts` Zod schema | Module-load validation | WIRED | Next.js booted `Ready in 88ms` with no `Environment validation failed:` error |
| CF Access JWT | `components/app-header.tsx` avatar | `lib/current-user.ts` reads verified claims | WIRED | Operator-observed: avatar shows `"M"` for signed-in Mike (NOT `"CD"` fallback) |
| `curl direct Railway origin` | Railway edge block (401) | Railway Public Networking config | WIRED | HTTP 401 + `server: railway-edge` + 12-byte body confirms edge-layer block, app never sees request |
| `.planning/DEPLOY.md` | `.planning/INITIAL_SEED_VERIFICATION.md` | Runbook cross-reference | WIRED | DEPLOY.md §4 references the artifact; grep returns 3 hits |

All 8 key links WIRED.

---

### Data-Flow Trace (Level 4)

Runtime artifacts verified for real data flow:

| Artifact | Data Source | Produces Real Data | Status |
|----------|-------------|---------------------|--------|
| Railway Postgres `drizzle.__drizzle_migrations` | drizzle-kit migrate (via prestart) | YES (6 rows, hashes match forward migrations) | FLOWING |
| Railway Postgres `tracks` + `stages` | `npm run db:seed` one-time | YES (8 + 25 rows match source files byte-for-byte) | FLOWING |
| `DATABASE_URL` on app service | Railway reference variable resolution | YES (resolves to real connection string at runtime, not literal `${{...}}`) | FLOWING |
| `<AppHeader>` avatar | CF Access JWT → `users` upsert → component render | YES (renders `"M"` for Mike, proves JWT parse → DB upsert → component read chain is functional end-to-end) | FLOWING |
| Direct Railway origin response | Railway edge infrastructure | YES (401 from `railway-edge`, not from app — confirms edge block active) | FLOWING |

All runtime data paths confirmed flowing.

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| package.json prestart script present | `node -e "console.log(require('./package.json').scripts.prestart)"` | `npm run db:migrate` | PASS |
| DEPLOY.md has 6 sections | `grep -c "^## " .planning/DEPLOY.md` | `6` | PASS |
| INITIAL_SEED_VERIFICATION.md outcome = verified | `grep -c "verified" .planning/INITIAL_SEED_VERIFICATION.md` | `3` (status + outcome checkbox + description) | PASS |
| Lock-file fix commits landed | `git log --oneline | grep -E "0c4d295|e6a3658"` | Both present | PASS |
| Phase closure commits landed | `git log --oneline | grep "00.5.1-03"` | 4 commits including `58cc1ae` (plan complete) | PASS |
| VERIFICATION.md has 7 checkpoints | grep `Checkpoint` in 00.5.1-VERIFICATION.md | Checkpoints 1-7 present | PASS |

All spot-checks pass.

---

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| CFA-11 | Plan 03 Task 3a | Direct Railway origin returns 403/404 (or equivalent blocked) | SATISFIED | HTTP 401 + `server: railway-edge` + 12-byte body. Semantically equivalent to spec's accept-list per Checkpoint 6 justification. Marked `[x]` in REQUIREMENTS.md line 29. |
| CFA-12 | Plan 03 Task 3b | Avatar renders real Google identity (not `"CD"` fallback) | SATISFIED (core invariant) | Mike signed in → avatar `"M"` (NOT `"CD"`). JWT parse → users upsert → component render chain functional. Marked `[x]` in REQUIREMENTS.md line 30. Single-initial formatting filed as follow-up todo (UI polish, non-blocking). |
| DEPLOY-07 | Plan 02 Checkpoints 1+2, Plan 03 runtime validation | Postgres provisioned + DATABASE_URL reference variable | SATISFIED | Postgres service `4e32c3b7-bbd2-4957-96bb-db8489248c2f` (Hobby). DATABASE_URL resolves as reference at runtime; Drizzle connected during prestart. Marked `[x]` in REQUIREMENTS.md line 172. |
| DEPLOY-08 | Plan 02 Checkpoint 3, Plan 03 runtime validation | NEXT_PUBLIC_APP_NAME/DOMAIN/BRAND_COLOR env vars set | SATISFIED | All 3 vars present with pinned canonical values. Next.js booted without Zod errors at runtime. Marked `[x]` in REQUIREMENTS.md line 173. |
| DEPLOY-09 | Plan 01 Task 1, Plan 03 runtime validation | Deploy-time migration hook (prestart or postdeploy) | SATISFIED | `package.json` prestart hook committed `41ecb85`; fired automatically during deploy `540160a4` and applied 6 migrations. Marked `[x]` in REQUIREMENTS.md line 174. |

All 5 requirements SATISFIED. No orphaned requirements: every Phase 0.5.1 REQ-ID in REQUIREMENTS.md lines 223-224 is claimed by a plan's `requirements` field.

---

### Anti-Patterns Scanned

Files touched in this phase (from SUMMARY key-files):
- `package.json` (+1 line for prestart)
- `.planning/DEPLOY.md` (new)
- `.planning/INITIAL_SEED_VERIFICATION.md` (new, filled)
- `.planning/phases/00.5.1-production-deploy-wire-up/00.5.1-VERIFICATION.md` (7 checkpoints appended)
- `package-lock.json` (regenerated + platform-flag patch across `0c4d295` + `e6a3658`)
- `.planning/REQUIREMENTS.md` (CFA-11/12 + DEPLOY-07/08/09 marked [x])
- `.planning/todos/pending/2026-04-18-*.md` (2 follow-up todos)

Anti-pattern scan results: **None found.**
- No TODO/FIXME/placeholder code added (only planning docs use those words in prose)
- No empty `return null`/`return {}` stubs introduced
- No console.log-only handlers added
- No hardcoded empty-array/empty-object props added
- package-lock.json changes are targeted fixes (dedupe + platform-flag correction), not stubs

---

### Phase 0.5 Topology Preservation

Verified that Phase 0.5 GLBA-scope infrastructure remained untouched:

| Invariant | Check | Status |
|-----------|-------|--------|
| CF Access + cloudflared tunnel unchanged | Plan 02 Checkpoint 3 preservation audit: `grep -cE "^(CF_ACCESS_TEAM_DOMAIN|CF_ACCESS_AUD|ENCRYPTION_KEY)="` returns 3 | PRESERVED |
| ENCRYPTION_KEY not read, not rotated, not clobbered | `railway variables --set` confirmed upsert semantics (not replace); Plan 03 Task 2 only did trailing-`=` sanity check (operator-observed, no key exfiltration) | PRESERVED |
| NPI crypto + audit scaffolding intact | No changes to `lib/crypto.ts`, `lib/access.ts`, `audit_log`, `npi_access_log` tables | PRESERVED |
| `portal.utstitle.com` routing via CF Access | CFA-11 verification confirmed CF-fronted domain returns 302 to login (expected); direct origin blocked at Railway edge | PRESERVED |
| 170/170 local tests green | Plan 01 baseline regression check; confirmed in SUMMARY | PRESERVED |

All P0.5 locks preserved.

---

### P0.5 Deferrals Closed

| Deferral | P05-CLOSURE Status (before) | P0.5.1 Closure | Plan Task |
|----------|------------------------------|----------------|-----------|
| SC1: Direct origin returns 403/404 | "⚠️ Not live-tested" | ✓ Live-tested 2026-04-18 — HTTP 401 from railway-edge | Plan 03 Task 3a (CFA-11) |
| SC3: Avatar shows real Google identity | "⚠️ Avatar showed 'CD' during smoke test" (suspected stale deploy) | ✓ Live-tested 2026-04-18 — avatar `"M"` for Mike, NOT `"CD"` | Plan 03 Task 3b (CFA-12) |
| SC4: MFA Require rule restored | "🔴 Temporarily disabled" | NOT IN SCOPE for P0.5.1 (continues as P0.5 compliance debt, to be addressed when 2SV enrollment completes) | N/A |

P0.5.1 closed both deferrals that were specifically in-scope.

---

## Follow-Up Todos (Legitimate Deferrals — NOT Gaps)

Two follow-up todos filed per operator instruction ("File both as follow-ups. Close P0.5.1 cleanly. Address both in a post-phase work batch.") — these are deliberate scope deferrals, not verification gaps:

### 1. `2026-04-18-fix-appheader-avatar-to-show-both-initials.md` (area: ui, LOW priority)

- **Why deferred:** CFA-12 invariant ("avatar reflects signed-in user, NOT fallback") PASSED. The single-character rendering (`"M"` vs `"MM"`) is UI polish, not a security invariant failure. Filed with rich context for `/gsd:fast` or `/gsd:quick` execution.
- **Commit:** `c21278c`
- **Not a verification gap** because the phase goal (auto-deploy pipeline + CF Access enforcement) is met.

### 2. `2026-04-18-investigate-cf-access-session-persistence-in-incognito.md` (area: auth, HIGH priority)

- **Why deferred:** Incognito CF Access session behavior is an orthogonal security investigation that needs evidence-gathering before fix scope can be determined (15-min config fix vs 1-day app-code fix). Operator instruction: "no parallel debug sessions, close P0.5 cleanly." Phase 1 investigation plan scoped in the todo itself.
- **Commit:** `00745a6`
- **Not a verification gap** for this phase because CFA-12 acceptance criterion ("avatar reflects signed-in user") is met. Session-persistence question does not block the phase goal. HIGH priority because it must be resolved before written GLBA Safeguards cites CF Access + MFA as a control.

Both todos are properly captured with:
- ISO timestamps
- Area classification
- Priority rating
- Rich context (problem, root-cause hypotheses, approach, recommended command)
- Cross-references to related threads
- Clear deferral rationale

---

## No Regressions

Verified absence of regressions:

| Check | Status |
|-------|--------|
| 170/170 local tests green (Plan 01 baseline) | PASS |
| `npx tsc --noEmit` clean (Plan 01) | PASS |
| `npm run build` clean (Plan 01) | PASS |
| `portal.utstitle.com` responding via CF Access (Checkpoint 6 cf-fronted probe → 302 to login) | PASS |
| Direct Railway origin blocked (Checkpoint 6 direct-origin probe → 401 from railway-edge) | PASS |
| P0.5 env vars preserved (Checkpoint 3 preservation audit → grep count 3/3) | PASS |
| No public exposure regression (body size 12 bytes of `Unauthorized`, no leaked content) | PASS |

---

## Gaps Summary

**No gaps.** All 7 success criteria VERIFIED with observable evidence. All 5 requirements SATISFIED. All key links WIRED. All P0.5 in-scope deferrals closed. No anti-patterns. No regressions. Phase 0.5 topology preserved. Two follow-up todos are legitimate operator-directed deferrals, not verification failures.

---

## Overall Status: PASSED

**Phase 0.5.1 goal achieved.** Railway production is now in a state where any `git push origin master` auto-deploys a healthy app with migrations run (via prestart), env vars resolved (via Railway reference vars + Zod validation), and CF Access still enforcing (topology preserved). P1+P2 can ship with a single `git push` — the "deploy plumbing carries the weight" invariant from the ROADMAP is live and proven end-to-end via deploy `540160a4`.

**Score:** 7/7 success criteria verified
**Requirements closed:** CFA-11, CFA-12, DEPLOY-07, DEPLOY-08, DEPLOY-09
**Follow-ups filed:** 2 (legitimate deferrals, not gaps)
**Human verification candidates:** None remaining — Tasks 3a and 3b already executed the only human-observable acceptance tests.

---

_Verified: 2026-04-18T20:10:00Z_
_Verifier: Claude (gsd-verifier)_
