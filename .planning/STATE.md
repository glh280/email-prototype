---
gsd_state_version: 1.0
milestone: v1.0
milestone_name: milestone
current_phase: 03
status: phase-complete
last_updated: "2026-04-19T02:40:00.000Z"
progress:
  total_phases: 14
  completed_phases: 0
  total_plans: 0
  completed_plans: 20
---

# Project State

**Milestone:** v1 — Core Dashboard MVP
**Current phase:** 03 (COMPLETE — awaiting /gsd:verify-phase + /gsd:transition to P4)
**Status:** Phase 03 COMPLETE (7/7 plans shipped)
**Last updated:** 2026-04-19 (rev 32 — P3 Plan 07 executed: D-03 CLOSED at data + write-path + UI layers. Migration 0007 ships idempotent SQL backfill (deals.main_contact_* → contacts + deal_people rows) using DISTINCT ON (lower(trim(email))) for dedupe (MIN(uuid) unavailable in Postgres) and ON CONFLICT DO NOTHING against the partial unique index + one-per-slot unique index. Legacy columns preserved with COMMENT ON COLUMN grace-period markers; DROP scheduled for P10 calibration. createDeal extended inside its existing db.transaction with dual-write: when mainContactEmail is non-empty, pre-read contact by lower(email), INSERT if absent (else reuse), INSERT deal_people row with role='main_contact', emit 2 new audit rows with source markers contact_create_via_new_deal + deal_people_upsert_via_new_deal. queryDealById extended with LEFT JOIN deal_people + contacts populating DealDetail.mainContactPerson. Overview Tracking card gains read-only Main Contact row via MainContactReadOnly component (3-state render: deal_people → legacy fallback → em-dash with 'Assign in File Contacts →' link). New Deal form intake UX UNCHANGED. Full suite 298/298 (+14: 8 backfill + 6 dual-write). tsc + build clean. npm run db:migrate idempotent on repeat runs. Commits: 573a32d / c3b2f5f / e80785a. Phase 3 complete — PEOPLE-01..05 + VIEW-06 delivered at data + UI.)

---

## Next Session — Priority Order (rev 25, 2026-04-18)

### 1. ▶ Execute Phase 3 (PRIMARY NEXT ACTION — everything else is queued behind this)

**Command:** `/gsd:execute-phase 03` — run `/clear` first for a fresh context window

**Status:** 7 plans ready, 5 waves, checker PASSED on first verification iteration (commit `6d9a53b`). 3,489 total lines of plan content across:

| Wave | Plans | Deliverable |
|------|-------|-------------|
| 1 | 03-01 | Drizzle schema + migration 0006 (contacts + deal_people tables) |
| 2 | 03-02, 03-03 | Role-slot registry + Zod contracts; read-query layer |
| 3 | 03-04 | 4 Server Actions with atomic audit + PEOPLE-05 role-track gate (TDD) |
| 4 | 03-05, 03-06 | `/contacts` page UI; deal-detail File Contacts tab (autosuggest + lender free-text) |
| 5 | 03-07 | D-03 backfill — `deals.main_contact_*` → `deal_people` FK migration |

Requirements covered: PEOPLE-01..05 + VIEW-06. Each at both data AND UI layers. No MFA refs, no `@/db/client`, no Auth.js.

**Pre-reqs:** none — P1 UAT automation blocked (see §2); operator can do manual UAT in parallel or defer until after P3.

### 1.5. ✅ CF Access security investigation — RESOLVED

Closed via `/gsd:debug` sessions 2026-04-17/18. Two distinct root causes surfaced and disposed:

- **Edge + Windows Credential Manager / WAM silent OAuth completion** → documented as working-as-designed OS behavior in `.planning/VENDORS.md` Cloudflare row + `.planning/debug/2026-04-17-portal-access-denied-resolved.md` (Option A chosen; Option C app-level session layer deferred).
- **CF Access sign-out did not fully clear app-domain session** → fixed in commit `cc2f459`: `proxy.ts` sets `Cache-Control: no-store, private`; `components/app-header.tsx` uses app-domain logout URL. Closes CFA-06. See `.planning/debug/2026-04-18-signout-does-not-clear-session-resolved.md`.

### 1.6. ⏳ Pending todos — 6 active + 1 done today

Captured before any `/clear` per operator checklist discipline. Pick via `/gsd:check-todos`:

| Risk | Todo |
|------|------|
| **Deployment-breaking** | `2026-04-18-strip-mfa-require-rule-from-p05-plan-runbook.md` |
| **Deployment-breaking** | `2026-04-18-correct-plan-cf-team-domain-placeholder.md` |
| **Narrative / compliance** | `2026-04-18-strip-mfa-references-from-planning-docs-20-files.md` |
| **Docs / strategic** | `2026-04-18-roadmap-content-reconciliation-mfa-cfa06-optionc.md` — ROADMAP content reconciliation (MFA deferral SC4, CFA-06 closure note, Option C placement decision) |
| **Regression defense** | `2026-04-18-add-regression-test-for-cache-control-header.md` |
| **Tests / automation** | `2026-04-18-playwright-uat-cf-session-replay-blocker.md` — Playwright UAT against CF-gated prod blocked on session-replay; 4 future options documented with revisit triggers |
| **Pre-existing (unchanged)** | `2026-04-18-fix-appheader-avatar-to-show-both-initials.md` — LOW priority UI polish |

Done today (moved to `.planning/todos/done/`):

- ✅ `2026-04-18-investigate-cf-access-session-persistence-in-incognito.md` — answered by Edge+WAM debug resolution
- ✅ `2026-04-18-remove-stale-prototype-banner-from-layout.md` — shipped in commit `5d9b054`

**Suggested batching:** first two (deployment-breaking) as one `/gsd:quick` before any new Access Application setup. Third + fourth as narrative refresh. Fifth as TDD add-on. Sixth when threat model tightens or test-automation value increases.

### 2. P1 + P2 human UAT — automation BLOCKED, manual still viable

Playwright UAT against `portal.utstitle.com` (CF-gated) was attempted via both service token AND cookie replay — both blocked at the CF layer (service-token policy binding indeterminate; cookie replay rejected because CFA-06 sign-out working correctly invalidated the copied cookie). Full investigation in the session-replay blocker todo.

- **P1 UAT file:** `.planning/phases/01-core-data-model/01-HUMAN-UAT.md` (3 items; Test 3 marked `blocked` with `blocked_by: cf-access-automation-session-replay`; Tests 1+2 remain `pending` awaiting manual operator review)
- **P2 UAT file:** `.planning/phases/02-deal-detail-tasks-stages/02-HUMAN-UAT.md` (5 pending items)
- **Harness artifacts shipped:** `playwright.uat.config.ts` + `tests/e2e/uat/p1-uat.spec.ts` + `lib/access.ts` service-identity support — all usable once a working auth path is established (storageState with full cookie dump is the most likely Option-1 path; see blocker todo)
- **Manual path:** operator clicks through UAT items in browser (~5-10 min each). Tests 1+2 are visual checks where eye-review catches regressions better than assertions anyway.

**Caveat for manual UAT in Edge:** Google sign-in step may complete invisibly via WAM. Use Chrome incognito or Edge Guest mode if visible account-chooser is required.

### 3. Phase 4+ — Gmail Integration + Intake (next after P3)

- **Start command:** `/gsd:plan-phase 4` after P3 completion + verification
- **Goal (from ROADMAP):** Pull Gmail threads on demand; three intake paths wired up (manual / Gmail URL / free-text paste)
- **Pre-reqs:** P3 People Map should close first — P4 Gmail intake creates contacts + assigns role slots as a side effect of intake, so the people map model must exist

### Process note — pre-/clear checklist (operator-directed 2026-04-18)

Before any `/clear` in this project, operator has mandated:

1. All findings from the current session that are not yet fixed must be written to `.planning/todos/pending/`
2. Any named fix candidate identified during debugging must be committed to the active debug file before context clears
3. Any unchecked requirement that surfaced during the session must have a todo filed against it
4. STATE.md must be updated with the current priority order and next steps
5. Confirm all todo files, debug files, and STATE.md updates are committed to git — not just written to disk
6. Only after git commit confirmation is `/clear` safe to run

**This checklist is mandatory. `/clear` without completing it is a context-loss event. Git is the memory.**

---

## Current Phase

**✅ Phase 0.5.1 (Production Deploy Wire-Up) complete 2026-04-18 — zero warnings, 7/7 success criteria, 5/5 requirements.**

portal.utstitle.com is now serving current P1+P2 code. Every `git push origin master` auto-deploys with migrations applied via `package.json prestart` hook. Railway has 3 services: `tunnel` (CF Access/cloudflared), `npr-dashboard-prototype` (Next.js app), `Postgres` (Hobby tier, private network).

**Verification:** `.planning/phases/00.5.1-production-deploy-wire-up/00.5.1-PHASE-VERIFICATION.md`

**Previous phases complete:** P0, P0.5, P1, P2 (all shipped).

---

## Active Blockers

None. CF Access investigation items resolved 2026-04-18:

- ✅ **CF Access session persistence in incognito** — resolved and moved to `.planning/todos/done/` (root cause: Edge+WAM silent OAuth, documented)
- ✅ **CF Access policy blocking mike@ / info@** — resolved; debug file renamed to `.planning/debug/2026-04-17-portal-access-denied-resolved.md`. Prior H1 (policy allowlist) was invalidated by evidence; actual root cause was the 2026-04-17 MFA Require rule (removed same day per `P05-CLOSURE.md` SC4)
- ✅ **CF Access sign-out did not fully clear session** — FIXED in commit `cc2f459` (Cache-Control on proxy.ts + app-domain logout URL in app-header.tsx). Closes CFA-06.
- ⏳ **Avatar initials rendering** — LOW priority UI polish (`.planning/todos/pending/2026-04-18-fix-appheader-avatar-to-show-both-initials.md`), still open

**Previously-completed prereqs (kept for historical reference):**

- ✅ Docker Desktop installed + running
- ✅ Local Postgres via `docker-compose` running
- ✅ GitHub repo `glh280/npr-dashboard-prototype` + pushed
- ✅ Cloudflare Zero Trust account + Access application + Google IdP + MFA + policy (B1)
- ✅ Cloudflare Tunnel token deployed as cloudflared sidecar on Railway (B2)
- ✅ `ENCRYPTION_KEY` 32-byte base64 in Railway env + 1Password backup (C2)
- ✅ Railway Postgres Hobby tier provisioned (P0.5.1 Checkpoint 1)
- ✅ `DATABASE_URL` reference variable wired (P0.5.1 Checkpoint 2)
- ✅ `NEXT_PUBLIC_APP_*` env vars set (P0.5.1 Checkpoint 3)

---

## Session Log

| Date | Event |
|------|-------|
| 2026-04-16 | Project initialized. Spec + P0 plan ported. `.planning/` structure bootstrapped. |
| 2026-04-16 | ClickUp Partner Profiles list created with 40 custom fields + BC-17 vetting gate. |
| 2026-04-16 | Pivoted to UI prototype path — Carrie feedback loop. Rev 1 → Rev 5 shipped to Railway. |
| 2026-04-16 | Rev 5 deployed after multiple Railway CLI `/up` timeouts; GitHub repo created as fallback. |
| 2026-04-16 | Prototype-to-production audit written (`AUDIT.md`). ~60 surfaces → ~18 backend modules. |
| 2026-04-16 | P0 plan rewritten (rev 6) to start from prototype state. |
| 2026-04-16/17 | P0 Tasks 1–7 executed: deps, env, Drizzle + Postgres, Auth.js + allowlist, sign-in page, proxy gate, Playwright smoke. Commits `fc8da45` through `14b902c`. |
| 2026-04-17 | **Spec update: Cloudflare Access + R2 + GLBA alignment.** P0 Auth.js work to be reverted by P0.5 Stage A. AUDIT.md revised (rev 7). P0.5 added to ROADMAP between P0 and P1. |
| 2026-04-17 | **AUDIT rev 8 — four fixes:** (1) encryption key custody → Railway env + 1Password (CF Secrets unreachable from Railway); (2) new Scope section (internal-only, ≤50 users); (3) Logpush dropped (Enterprise-only) — layers 1+2 authoritative, CFA-10 deleted; (4) P5.5 Documents + R2 inserted between P5 and P6. |
| 2026-04-17 | **P0.5 executed end-to-end (11 commits).** Stage A: next-auth/drizzle-adapter uninstalled, jose promoted; Auth.js files deleted; proxy no-op'd; Auth.js tables dropped. Stage B: cloudflared sidecar deployed (distroless image, TUNNEL_TOKEN env var); lib/access.ts with TDD JWKS-based JWT verification (6/6 tests); lib/users.ts; proxy rewritten for Access enforcement; lib/current-user.ts; app-header reads real identity. Stage C: npi_access_log table; @47ng/cloak installed; lib/crypto.ts with non-skippable decrypt() → audit-row invariant (2 invariant tests passing). VENDORS.md verified. portal.utstitle.com live behind Cloudflare Access + MFA. Next: Phase 1 (Core Data Model). |
| 2026-04-17 | **P1 Plan 01 executed (3 commits + TDD).** `db/schema.ts` split into `db/schema/{auth,app,index}.ts` per D-04 (template-extraction seam). 4 new tables landed via migration 0003: tracks (6 cols, 1 CHECK), stages (6 cols, FK→tracks nullable), deals (35 cols, 2 CHECKs, 4 FKs, 6 idx), audit_log (9 cols, 1 CHECK, FK→users, 3 idx). D-04a FK on `npi_access_log.deal_id → deals.id` closes P0.5's dangling reference. 7 TDD schema-shape tests + full 23/23 green; `npm run build` + `npx tsc --noEmit` clean. Commits `2c04d24` (test RED) → `b28ec4b` (feat GREEN schema split) → `83cc000` (feat migration 0003). DB-client convention locked: `@/lib/db` is canonical, `@/db/client` forbidden. Requirements DEAL-01, DEAL-02, DEAL-02a, STAGE-01, OPS-01 marked complete. Next: Plan 02 (indexes + stage-guard CHECK). |
| 2026-04-17 | **P1 Plan 02 executed (3 commits + TDD).** D-06 landed end-to-end: `NEXT_PUBLIC_APP_NAME` (required), `NEXT_PUBLIC_APP_DOMAIN` (required, bare-domain regex), `NEXT_PUBLIC_APP_BRAND_COLOR` (optional) added to `lib/env.ts` Zod schema. 5 new env test assertions (10/10 green in that file). `components/app-header.tsx` rewritten to render `{env.NEXT_PUBLIC_APP_NAME}`; "prototype" sub-label dropped per D-10. `.env.example` + `.env.local` both document the new vars. Rule-3 fix: `tests/unit/crypto.test.ts` `beforeEach` extended with the new required vars (module-load regression caught by full-suite run). Full suite 28/28 green; `npm run build` + `npx tsc --noEmit` clean. Commits `65e598c` (test RED) → `0e18e3a` (feat GREEN schema + env.example + crypto-test fix) → `9ddba76` (feat AppHeader). OPS-01 already complete from Plan 01. Duration ~3m 24s. Next: Plan 03 (file_no generator). |
| 2026-04-17 | **P1 Plan 03 executed (4 commits + TDD).** DEAL-02a landed end-to-end. Migration 0004 adds plpgsql function `next_file_no(state_code text)` with lazy `CREATE SEQUENCE IF NOT EXISTS deals_file_no_{YYYY}_seq`, null/empty → 'XX' normalization, `upper(trim(...))` otherwise, and `lpad(nextval::text, 4, '0')` zero-pad. `lib/file-no.ts` exports `generateFileNo(tx, state)` + `AppTx = PgTransaction<NodePgQueryResultHKT, typeof schema, ExtractTablesWithRelations<typeof schema>>` — the proper drizzle-orm 0.45.2 tx type, not the loose duck-type `{ execute }`. 8 real-Postgres tests cover: TX clean → 0001; sequential same-state; interleaved states share counter (global-within-year); null/empty → XX; lowercase normalized; pg_sequences name check; 10-way concurrency atomicity. Full suite 36/36 green; `npm run build` + `npx tsc --noEmit` clean; `npm run db:migrate` idempotent. Commits `521e965` (test RED) → `3b061f2` (feat SQL migration) → `2de5b1a` (feat wrapper) → `1f0cb3f` (refactor comment rephrase). Duration ~6m. Next: Plan 04 (seed tracks + stages). |
| 2026-04-17 | **P1 Plan 04 executed (2 commits).** DEAL-01 + STAGE-01 data landed end-to-end. `db/seed/tracks.ts` exports `TRACK_SEEDS` (8 rows; TE/FL=HIGH, DP/PO/EC/SL/BL/GI=MEDIUM) and `db/seed/stages.ts` exports `STAGE_SEEDS` (25 rows; 4 universal + 10 TE + 11 FL, sort_order 10..40 / 100..190 / 200..300). `db/seed/run.ts` uses `onConflictDoUpdate` keyed by `code` — idempotent on re-run (both runs exit 0 with "Seeded 8 tracks, 25 stages."). Seeder resolves stage FKs via code→id map read back after track upsert; throws on invariant violation. `npm run db:seed` wired via `tsx` in package.json. Rule 3 fix: top-level `import` of `@/lib/db` hoisted above `config()`, triggering Zod env-validation failure on first run — moved DB/schema/seed imports to dynamic `await import(...)` inside `main()` (documented in seeder JSDoc). SQL-verified counts: tracks=8, stages=25, universal=4, TE=10, FL=11, terminal=2. 13 seed-shape unit tests encode DEAL-01 + STAGE-01 verbatim; full suite 49/49 green; `npm run build` + `npx tsc --noEmit` clean. Commits `d139716` (feat seeder) → `de10846` (test seed-shape). Duration ~3m 34s. Next: Plan 05 (new-deal form). |
| 2026-04-17 | **P1 Plan 05 executed (7 commits + TDD).** DEAL-02 + DEAL-02a + DEAL-03 + OPS-01 shipped end-to-end. `/deal/new` route: server-component `page.tsx` + client `new-deal-form.tsx` (react-hook-form + zodResolver + shadcn accordion; Track `<SelectItem>` color dots; $-prefix money inputs wire `onBlur` via `makeMoneyBlurHandler` → `parseMoney`; Popover+Calendar date pickers; sticky footer). `app/deal/new/actions.ts::createDeal` wraps `generateFileNo` + deal INSERT + `writeAuditLog` in ONE `db.transaction` — Test 7 enforces the rollback invariant (mock audit throw ⇒ no deal row). `lib/deal-schema.ts` exports `createDealSchema` + split input/output types (`CreateDealFormInput` via `z.input` for RHF state, `CreateDealInput` via `z.infer` for action output — needed because `z.boolean().default(false)` diverges input/output). `lib/audit.ts::writeAuditLog(tx, ...)` is transaction-scoped + denormalizes `userEmail` per D-05. `lib/parse-money.ts` with 13 behaviors green (null/empty/comma/$/whitespace/zero/decimal-truncate/garbage/negative). Manual shadcn Form primitive (base-nova ships no `form` — wrote it by hand mirroring shadcn/ui API against RHF 7.72). Rule 3: `vitest.config.ts` → `fileParallelism: false` (DB sequence races between file-no + create-deal suites). Full suite 71/71 green (49 baseline + 13 parse-money + 9 create-deal-action); `npm run build` registers `/deal/new`; `npx tsc --noEmit` clean. Commits `378c1b5` (parse-money RED) → `aef00ae` (parse-money GREEN) → `67d9f58` (createDeal RED) → `848b70b` (createDeal GREEN — schema + audit + action) → `dd83f05` (vitest serial) → `10668f7` (shadcn primitives + form.tsx) → `64765a2` (page + new-deal-form). Duration ~13m. Next: Plan 06 (list view rewrite). |
| 2026-04-18 | **P2 Plan 04 executed (2 commits + TDD).** STAGE-02 + STAGE-03 + DEAL-05 + DEAL-06 shipped. `app/deal/[id]/actions/stages.ts` exports `advanceStage` / `revertStage` top-level Server Actions + `advanceStageInTx` / `revertStageInTx` tx-scoped helpers (Plan 03's completeTask will compose advanceStageInTx for auto-advance). Shared `applyStageTransitionInTx` core validates direction (target.sort_order > current for advance, < for revert) + track compatibility (target.track_id IS NULL OR = deal.track_id). `app/deal/[id]/actions/deals.ts` exports `updateDeal` + `closeDeal` + `killDeal`. `updateDeal` uses split-parse (dealIdSchema + updateDealSchema.safeParse on remainder) because `z.object({}).and(updateDealSchema)` does NOT propagate `.strict()` unknown-key rejection — this is the canonical pattern for future composite-input Server Actions. Empty-diff short-circuits to `{ok:true, noop:true}` with NO audit row. `killDeal` writes 4 rows in ONE tx: UPDATE deals (status/stage/killedAt/closedAt/killReason) + INSERT deal_notes (is_system=true) + 2 audit rows (deals.update + deal_notes.create). Kill-rollback invariant proven by Test 9 (`vi.doMock('@/lib/audit')` → writeAuditLog throws → deal stays status='active', no system note leaks). Audit `source` marker in afterJson vocab: manual_advance / manual_revert / manual_close / manual_kill / task_autoadvance / task_autoadvance_undo — gives the Audit-tab filter (Plan 06) a canonical filter vocab. `revalidatePath('/deal/[id]', 'page')` per Next.js 16 dynamic-route requirement. 19 new real-Postgres tests (10 stage + 9 deal); full suite 134 → 153 green; tsc + build clean. Rule 1 deviation auto-fixed inline: `.and()` composition did not propagate `.strict()` → switched to split-parse (documented in module docstring + SUMMARY). Commits `788b4ec` (test RED — both test files fail with module-not-found) → `f38f2a7` (feat GREEN — stages.ts + deals.ts with split-parse). Duration ~10m. Requirements DEAL-05 + DEAL-06 + STAGE-02 + STAGE-03 complete. Next: P2 Plan 03 (task server actions — consumes advanceStageInTx from this plan for TASK-01 auto-advance). |
| 2026-04-18 | **P2 Plan 02 executed (4 commits + TDD).** DEAL-05 + DEAL-06 + STAGE-02 + STAGE-03 + TASK-01 Zod contracts landed. `lib/deal-schema.ts` extended with `updateDealSchema` (20 optional fields + `.strict()` to reject immutable trackCode/fileNo + status enum `'active'|'closed'` only — killed forced through killDealSchema) + `killDealSchema` (uuid dealId + reason trimmed min 3 / max 2000). Three new sibling modules: `lib/task-schema.ts` (createTaskSchema + updateTaskSchema `.strict` + completeTaskSchema — z.input/z.infer split on `isNext` default false), `lib/stage-schema.ts` (advanceStageSchema + revertStageSchema — identical `{dealId, targetStageId}` shapes), `lib/note-schema.ts` (createNoteSchema — trim + min 1 / max 5000). 43 co-located behavioral tests (15 deal + 14 task + 8 stage + 6 note) — pure Zod, no DB, no mocks. `mainContactName`/`mainContactEmail` deliberately omitted from updateDealSchema per B1 revision (DB columns exist for P1 createDeal but editing migrates to P3 contacts FK — D-03); verified `grep "mainContact" lib/{task,stage,note}-schema.ts` returns 0 matches. Full suite 91 → 134 green; `npx tsc --noEmit` clean; `npm run build` 4 routes. Commits `927ba0a` (test RED — deal) → `c5f5d7f` (feat GREEN — updateDealSchema + killDealSchema) → `2b81162` (test RED — task/stage/note) → `d235f6d` (feat GREEN — task + stage + note schemas). Duration 5m 7s. Zero deviations. Next: P2 Plan 03 (task server actions — create/complete/reassign; consumes createTaskSchema + updateTaskSchema + completeTaskSchema). |
| 2026-04-18 | **P2 Plan 01 executed (2 commits + TDD-style).** DEAL-06 + TASK-01 + TASK-02 schema landed. `db/schema/app.ts` gains `tasks` table (13 cols, CHECK on status ∈ {open,done,skipped}, 5 FKs with deal_id CASCADE + parent/advances_stage SET NULL + user refs NO ACTION, 3 btree indexes + PARTIAL UNIQUE INDEX `tasks_one_is_next_per_deal_idx ON tasks (deal_id) WHERE is_next = true`); `dealNotes` table (6 cols, composite deal_id+created_at index, append-only); nullable `killedAt` + `killReason` columns added to `deals`. New type exports: `Task`, `NewTask`, `DealNote`, `NewDealNote`. `uniqueIndex` + `AnyPgColumn` imported from `drizzle-orm/pg-core`. Schema-shape test grew 7 → 16 assertions (P1 7 + P2 9): exports, column lists, default values, FK nullability, runtime type-instantiation proxy. Migration generated via `npx drizzle-kit generate` (auto-tag `0005_far_skullbuster`) then renamed → `0005_phase2_tasks_notes_kill.sql` with matching `_journal.json` tag edit. `npm run db:migrate` exits 0 first+second run (idempotent). DB smoke-test confirmed partial unique index enforces TASK-02 at the DB level: first is_next=true accepted, second rejected with `unique_violation`, is_next=false rows unconstrained. Full suite 82 → 91 green; `npx tsc --noEmit` clean; `npm run build` 4 routes. Commits `2bbe762` (test RED — schema + test assertions) → `d79b719` (feat GREEN — migration 0005). Duration 4m 41s. Zero deviations. Next: P2 Plan 02 (next-task query + list-view wire-through). |
| 2026-04-18 | **P2 Plan 03 executed (2 commits + TDD).** TASK-01 + TASK-02 + TASK-03 + TASK-04 + TASK-05 shipped. `app/deal/[id]/actions/tasks.ts` (469 lines) exports 5 Server Actions: `createTask` (INSERT + atomic clear-prior-is_next + audit), `completeTask` (done + auto-promote + optional advanceStageInTx for TASK-01 auto-advance + triggeringTaskId breadcrumb merge via `after_json \|\| jsonb_build_object('triggeringTaskId', ...)`), `undoCompleteTask` (audit-lookup revert flow — SELECTs most recent `task_autoadvance` audit row WHERE `afterJson->>'triggeringTaskId' = taskId`, reads `beforeJson.stage_id`, calls `revertStageInTx` with `source:'task_autoadvance_undo'`; aborts with `Cannot safely revert stage` + tx-rollback if audit row missing — proven by Test 8c), `updateTask` (partial diff + clears prior is_next if isNext:true), `reassignTask` (sugar over updateTask). `lib/tasks-query.ts` ships `queryTasksForDeal` — ONE SELECT with leftJoin(users), ORDER BY is_next DESC + due_date ASC NULLS LAST + created_at ASC; in-memory split into {open, done}; done re-sorted by completedAt DESC. All 5 mutating actions wrap DML + audit in ONE `db.transaction` (5× `db.transaction`, 9× `writeAuditLog` per grep). CompleteTaskResult contract: `stageAdvanced: { toCode, toLabel } \| null` populated by joining `stages` in the completion tx; `newIsNext: { id, title } \| null` populated by reading the newly-promoted task's returning row. 13 real-Postgres tests including INVARIANT 10-way `Promise.allSettled(createTask({isNext:true}))` proving exactly 1 is_next=true row survives (partial unique index `tasks_one_is_next_per_deal_idx` backstops app logic). Rule 1 deviations: (1) Test 10 user-cleanup FK ordering fixed (delete task first, then user, per NO ACTION on tasks.owner_user_id_users_id_fk); (2) CreateTaskResult 3-way discriminated union (validation errors vs runtime errors) narrowed via `"errors" in result` in Test 4. Full suite 153 → 166 green; tsc + build clean (4 routes). Commits `a7b9b1f` (test RED — module-not-found) → `8b849b8` (feat GREEN — tasks.ts + tasks-query.ts). Duration ~6m. Requirements TASK-01, TASK-02, TASK-03, TASK-04, TASK-05 complete. Next: P2 Plan 05 (deal detail shell + Overview UI) or Plan 06 (Notes + Audit tabs) — both wave 3, unblocked. |
| 2026-04-18 | **P2 Plan 05 executed (3 commits).** DEAL-04 + DEAL-05 + STAGE-02 + STAGE-03 + VIEW-05 shipped (DEAL-06 already complete from Plan 04). Full deal-detail UI: Server Component `app/deal/[id]/page.tsx` awaits Promise-shaped params+searchParams, calls `queryDealById`, delegates to 'use client' subcomponents inside `max-w-[1120px]` container. `lib/deals-query.ts` gains `queryDealById` + `DealDetail` type (joins deals + tracks + current stage; second SELECT for availableStages ordered by sortOrder, filtered to universal + deal-track). 9 new components under `app/deal/[id]/_components/`: deal-header (back link + title + track/priority/status badges + Edit + overflow DropdownMenu with Mark as closed + Kill deal…), stage-stepper (pip-and-line strip with HoverCard tooltips, past pips clickable → revert dialog, current primary-filled with ring, future disabled, terminal Check/X icons, Advance CTA right-aligned), deal-tabs (6 tabs VIEW-05 order with URL-state ?tab=<slug>, tolerant parser, placeholders for contacts/tasks/emails/notes/audit that Plan 06 replaces), overview-tab (4 stacked cards in VIEW-05 mandated order — Tracking FIRST — each a reusable `<OverviewCard>` with FieldSpec[]), overview-card (read mode <dl> grid with em-dash/mono, edit mode RHF + zodResolver(updateDealSchema), Save computes diff → updateDeal → toast `Changes saved.` + router.refresh), 4 mutation dialogs (advance/revert/close/kill — copy verbatim from UI-SPEC Copywriting Contract; kill requires reason ≥3 chars with client-side killDealSchema pre-validate). 3 shadcn primitives installed (alert-dialog, textarea, scroll-area — base-nova shipped all three without fallback). Not-found page (FileQuestion + "It might have been killed or the link is stale."). Rule 1 auto-fix: base-ui `AlertDialogDescription` renders as `<p>`, does NOT accept `asChild` — structured `Current: / Next:` block moved OUTSIDE Description (sibling <div>) to avoid invalid <div>-in-<p> nesting; tsc caught it. Zero `mainContact*` references in `app/deal/[id]/` (B1 holds). Full suite 166/166 green (no new tests — UI composition plan); npx tsc --noEmit clean; npm run build 4 routes. Commits `e516470` (shadcn + queryDealById) → `ad6ed22` (page shell + dialogs) → `296feb3` (overview tab). Duration ~20m. Requirements DEAL-04, DEAL-05, STAGE-02, STAGE-03, VIEW-05 complete (DEAL-06 already marked from Plan 04). Next: Plan 06 (Tasks + Notes + Audit tab content — replaces Plan 05's 3 placeholder panels). |
| 2026-04-18 | **P0.5.1 Plan 01 executed (2 commits).** DEPLOY-09 shipped + runbook scaffolded. `package.json` gains `"prestart": "npm run db:migrate"` between `build` and `start` — locked per CONTEXT §Migration Hook Style (platform-agnostic npm lifecycle hook; Railway keeps old deploy if prestart fails). Local verification exhaustive: (1) idempotent noop against already-migrated DB exits [✓], (2) `DROP DATABASE npr_dashboard` + `CREATE DATABASE` + migrate → `SELECT COUNT(*) FROM drizzle.__drizzle_migrations` = 6 (all 0000..0005 applied), (3) `npm start` full-chain captured — `prestart` → `db:migrate` → `next start` → "Ready in 685ms". `.planning/DEPLOY.md` landed with 6 sections: push-to-prod playbook, env-var inventory table (DATABASE_URL/CF_ACCESS_*/NEXT_PUBLIC_APP_*/ENCRYPTION_KEY/GMAIL_OAUTH_*), 3-layer rollback (app-only via Railway Deployments / Postgres snapshot restore / forward-compensating migration — drizzle-kit down-migration limitation acknowledged), initial-seed checklist, CFA-11 (curl direct-origin) + CFA-12 (avatar real user) closure verifications, compliance notes. Prestart-only migration policy explicitly recorded in §1 + §4 (operators do NOT run db:migrate from laptop against prod). `.planning/INITIAL_SEED_VERIFICATION.md` template ready for Plan 03: run metadata + row-count table (8 tracks / 25 stages / 4 universal / 10 TE / 11 FL) + field-level diff sections + outcome enum (verified / mismatch-resolved / mismatch-escalated) + node-e + pg readback commands (no psql fallback, pg v8.20.0 is a dep). Zero prod contact — no `railway` CLI invoked. Full suite 170/170 green (post seed restore after DROP DATABASE test); `npx tsc --noEmit` clean; `npm run build` clean (4 routes). Commits `41ecb85` (feat prestart hook) → `10c5817` (docs DEPLOY.md + template). Duration 15m 35s. Zero deviations. Requirements DEPLOY-09 complete. Brand-color canonical default `#0F172A` confirmed pin target for Plan 02 Task 3 (.env.local + .env.example both have `NEXT_PUBLIC_APP_BRAND_COLOR=` empty). Next: Plan 02 (Railway Postgres provision + env vars + first prod deploy). |
| 2026-04-18 | **P2 Plan 06 executed (3 commits + TDD). Phase 2 COMPLETE.** DEAL-04 + TASK-01..05 + VIEW-05 finalized end-to-end. Three tabs wired from scratch: **Tasks** (OPEN/DONE sections; is_next row carries `border-l-2 border-primary` accent bar + `◀ Next` chip; checking fires 5-second toast with Undo; 6-field New Task dialog with reachable-stages filter excluding `killed`); **Notes** (Textarea composer + newest-first Card list; `createNote` Server Action wraps INSERT + writeAuditLog in ONE db.transaction per D-05; rollback invariant proven by Test 3 with `vi.doMock("@/lib/audit")` → throw → note row absent); **Audit** (reverse-chron rows with 5 URL-state filter chips All/Deal/Tasks/Notes/Stage; before→after diff rendering with `→` arrow glyph, italic muted `null`, money/date formatters; `IGNORE_KEYS` filters audit-internal noise `source`/`triggeringTaskId`; empty-filter state with `Show all` clears `?auditFilter`). Stage filter SQL uses `IS DISTINCT FROM` on `beforeJson->>'stage_id'` vs `afterJson->>'stage_id'` to handle NULL-to-value transitions explicitly. **W2/W3 contract compliance proven by grep:** `result.stageAdvanced.toLabel` (NOT toCode) + `result.newIsNext.title` + `result.newIsNext?.id ?? null` passed to undoCompleteTask — all 2 matches each in task-row.tsx. `page.tsx` now Promise.all fetches 4 readers in parallel (`queryTasksForDeal` + `listUsers` + `queryNotesForDeal` + `queryAuditForDeal(id, filter)`). `parseAuditFilter` tolerantly parses `?auditFilter=` with unknown-value fallback to `all`. Full suite 166 → 170 green (+4 note-action tests); tsc + build clean (4 routes). Commits `e66ce71` (queries + createNote + tests) → `89d7ce2` (tasks tab + task-row + dialog + page partial) → `a6e93e8` (notes/audit tabs + deal-tabs final wiring + page Promise.all). Duration ~11m. Zero deviations. Phase 2 success criterion 5 proven end-to-end: every mutation from Plans 03/04/05/06 appears in the Audit tab with full before/after. **Ready for Phase 3 transition** (contacts tab + per-deal role slots). |
| 2026-04-17 | **P1 Plan 06 executed (5 commits + TDD).** VIEW-01 + VIEW-02 shipped. `app/page.tsx` rewritten as Server Component: parses Next.js 16 Promise-shaped `searchParams` → `parseFilterParams` → `queryDeals(filters, user.id)` → renders 10-column shadcn Table with 4 sortable headers (Activity/Task Due By/Priority/File #) + 8-filter URL-state bar + two empty states (FolderOpen "No deals yet" / Filter "No deals match your filters") + footer count + success toast. `lib/deals-query.ts` uses Drizzle `$with` CTE for `MAX(audit_log.created_at)` per deal (D-15) with `coalesce(..., updated_at)` fallback; inArray for OR-within-multi + and(...) for AND-across-groups (D-12); activity_desc default sort (D-13); Needs-me = `internalOwner = currentUser.id` (D-14). `lib/filter-params.ts` tolerantly parses 8 filters + sort (invalids dropped); serializer omits defaults so URL stays tidy; 11 unit tests green. `lib/format.ts` centralizes trackBadgeClasses (8) + priorityPillClasses/priorityDotClasses (3) + relativeTime (2m/3h/yesterday/Apr 14/Jan 2025 buckets) + truncate. 4 client subcomponents in `app/_components/`: deals-table (sortable headers), deals-filter-bar (Popover+Checkbox multi-select; 3 Popover+Calendar date ranges; UserCheck/AlertCircle icons), deal-row (full-row router.push + File # Link e.stopPropagation; em-dash in text-muted-foreground/70 for P2/P3 columns per D-16), success-toast (reads ?created=, fires once via firedRef guard, strips param via router.replace). `app/deal/[id]/page.tsx` neutralized in separate commit (Task 3A) BEFORE mock/ deletion (Task 3) for bisect safety. Rule 3 deviations: (1) deleted entire 16-file orphaned prototype-component graph (plan listed only 2; leaving the other 14 broke npm run build because they still imported @/mock); (2) base-nova Button doesn't accept `asChild` — used `render={<Link/>}` pattern Plan 05 established. Full suite 82/82 green (71 baseline + 11 new); npm run build exits 0 with 4 routes; npx tsc --noEmit clean. Zero `@/mock` and zero `@/db/client` imports remain anywhere. Commits `80fde7c` (test RED) → `17963cc` (filter-params GREEN) → `0c45810` (queryDeals + format) → `e2220ce` (Task 3A neutralize detail page) → `616378f` (Task 3 list view + delete mock + orphans). Duration ~12m. Requirements VIEW-01 + VIEW-02 complete. Phase 1 has shipped all 6 plans — ready for phase transition. |
| 2026-04-18 | **`/gsd:debug` session — CF Access investigation resolved end-to-end.** Two distinct root causes identified and disposed separately. **(1) Edge+WAM silent OAuth completion:** Windows Credential Manager / Web Account Manager silently replays credentials during CF Access's Google OAuth redirect on Edge; observable via JWT `iat: fresh + amr: absent` signature proving CF minted a JWT from cached team-level identity rather than running fresh OAuth. Documented as working-as-designed OS behavior in `.planning/VENDORS.md` Cloudflare row + `.planning/debug/2026-04-17-portal-access-denied-resolved.md`. Option A (document) chosen; Option C (app-level session layer using shape of "sat aside" P0 Auth.js specs) deferred. **(2) Sign-out did not fully clear session:** team-level logout URL (`glh280.cloudflareaccess.com/cdn-cgi/access/logout`) didn't clear the app-domain `CF_Authorization` cookie; compounded by zero `Cache-Control` headers on authenticated responses (browser bfcache could serve stale authenticated HTML post-signout). **Fixed in commit `cc2f459`:** `proxy.ts` now sets `Cache-Control: no-store, private` on success path; `components/app-header.tsx` uses app-domain relative logout URL (`/cdn-cgi/access/logout`). Full suite 170/170 green, tsc + build clean. Closes **CFA-06**. Pushed to master, Railway auto-deployed. **6 loose-ends captured as todos before /clear** (strip MFA from PLAN.md runbook, correct stale CF team domain placeholder, broader MFA docs scrub ~20 files, remove stale PROTOTYPE banner from layout.tsx, add Cache-Control regression test, pre-existing avatar initials). Operator also codified a **mandatory pre-/clear checklist** (findings → todos; fix candidates → debug file; unchecked requirements → todos; STATE.md update; git commit verify — recorded in §Next Session above). Process lesson: investigation knowledge that lives only in conversation context is effectively in write-only memory; persistence must be to `.planning/` artifacts + git before `/clear`. |
| 2026-04-18 | **Prototype banner removed from `app/layout.tsx` (commit `5d9b054`).** Stale banner claimed "PROTOTYPE — mock data only · no login · no database · no integrations · actions are simulated" — all four false post-P0.5, GLBA-hostile if seen by compliance reviewer. Also updated `metadata.title` from `"NPR Dashboard (prototype)"` to `"NPR Dashboard"` and `metadata.description` from prototype-era placeholder to scope-accurate description. 170/170 tests still green. Run as inline `/gsd:quick` before P1 UAT attempt, so UAT would validate real app state. |
| 2026-04-18 | **ROADMAP.md structural reconciliation (commit `71cbe1b`).** Fixed 4 mechanical drift points: header phase count 13 → 14 (P0.5.1 was inserted 2026-04-18 but header wasn't bumped); header requirement count 76 → 81 (P0.5.1 added DEPLOY-07..09 + CFA-10..11); Overview table missing P0.5.1 row — inserted between P0.5 and P1; P2 Overview table row had broken column shape ("Complete | 2026-04-18" replacing Requirements + Success criteria columns) — restored to 5-column structure. Acceptance Gate requirement count also reconciled (72 → 81). Three content decisions (MFA SC4 status, CFA-06 closure note, Option C placement) deferred to a new todo `2026-04-18-roadmap-content-reconciliation-mfa-cfa06-optionc.md` per operator direction "don't make the Option C strategic decision mid-session." |
| 2026-04-18 | **Playwright UAT attempt against `portal.utstitle.com` — both auth paths blocked at CF layer.** Path A (service token): built full harness with service-identity support in `lib/access.ts` (synthesizes `service:{common_name}@cf-access` email from JWTs lacking `email` claim — 4 new passing unit tests, 174/174 total green; shipped in commit `98e6b15`). Service token credentials added to `.env.local`. CF returned 302 → Google IdP with `service_token_status: false` even after operator added a CF dashboard policy. Diagnosed as policy-binding indeterminacy (Service Auth action vs Allow action). Path B (cookie replay): harness pivoted to `context.addCookies()` with `CF_Authorization` cookie pasted from operator's live browser into `.env.local` (shipped in commit `8e0c800`). CF rejected with `auth_status: NONE` — cookie structurally valid but server-side session invalidated. **Most likely cause of Path B block:** operator's session was killed by testing the CFA-06 sign-out fix earlier in the same session. Our working sign-out fix is literally what blocks this replay — security-correct, but blocks automated UAT. **Operator direction:** stop debugging auth, pivot to P3. Full investigation + 4 future path options (Playwright `storageState` with full cookie dump, Claude-in-Chrome MCP, redo service token, accept manual UAT) captured in `.planning/todos/pending/2026-04-18-playwright-uat-cf-session-replay-blocker.md` with revisit triggers. P1 UAT Test 3 marked `blocked`; Tests 1+2 remain `pending` awaiting manual operator review. The harness code (`playwright.uat.config.ts`, `tests/e2e/uat/p1-uat.spec.ts`, service-identity in `lib/access.ts`) is usable once a working auth path is established. |
| 2026-04-18 | **`/gsd:plan-phase 3` — Phase 3 People Map & Contacts Registry PLANNED (commit `6d9a53b`).** 7 plans in 5 waves, 3,489 total lines. First planner session disconnected at 4-plan mark after ~17 min (connection error); continuation planner wrote plans 05-07 (~9 min, 17 tool uses). Checker passed on first verification iteration — zero revisions needed, all 7 verification dimensions pass. Plan structure: **Wave 1** 03-01 (Drizzle schema + migration 0006 — `contacts` + `deal_people` tables with unique index `deal_people_one_per_slot_idx`, PEOPLE-01/02). **Wave 2** 03-02 (role-slot registry + Zod contracts; `lib/role-slots.ts` `ROLE_SLOTS` 12-entry registry with per-track applicability + `isRoleValidForTrack` pure function; PEOPLE-02/05) + 03-03 (read-query layer — `queryContactsForList` with activeDealsCount CTE, `queryContactsAutosuggest` with exact>prefix>substring ranking, `queryDealPeopleForDeal` join; PEOPLE-03/04 + VIEW-06). **Wave 3** 03-04 (4 Server Actions TDD — createContact/updateContact/upsertDealPerson/removeDealPerson; each wraps DML + audit in ONE `db.transaction`; PEOPLE-05 role-compatible-with-track gate with exact error string `Role "${role}" is not valid for ${trackCode} deals.`; rollback invariant via vi.doMock throw; PEOPLE-01/02/05). **Wave 4** 03-05 (/contacts page UI — table + search + New Contact dialog mirroring P1 Plan 06 pattern, URL-state `?q=<query>`, empty states, PEOPLE-04 + VIEW-06) + 03-06 (deal-detail File Contacts tab — replaces P2 placeholder, autosuggest combobox, lender_partner free-text distinct from mortgage_partner per REQUIREMENTS.md line 85, PEOPLE-02/03). **Wave 5** 03-07 (D-03 backfill migration 0007 — idempotent SQL with ON CONFLICT DO NOTHING, preserves deals.main_contact_* TEXT columns via COMMENT ON COLUMN grace-period markers, createDeal dual-write extension in existing tx; PEOPLE-02). Plans 05+06 exceeded 500-line soft target (757, 768) — intentionally verbose for Next.js 16 + base-nova primitive correctness (executor inferring would miss `render=` vs `asChild`, Promise-shaped `searchParams`, or lender free-text branch). Next.js 16 patterns verified via `node_modules/next/dist/docs/` per AGENTS.md mandate. No MFA refs, no `@/db/client`, no Auth.js. Additive cross-plan coupling flagged inline (NewContactDialog prop extension from Plan 05 to support Plan 06 embedded use — documented as executor-records-in-SUMMARY decision, additive only, no circular dep). Ready for `/gsd:execute-phase 03` — recommend `/clear` first for fresh context window given today's accumulated session load. |
| 2026-04-18 | **P3 Plan 02 executed (4 commits + TDD). Wave 2 partial — 03-02 complete, 03-03 remaining.** PEOPLE-02 (application-code taxonomy) + PEOPLE-05 (enforcement primitive) shipped. `lib/role-slots.ts` (162 lines) ships the canonical 12-entry `ROLE_SLOTS` registry with per-track applicability matrix, `source: "contact_fk" \| "free_text"` metadata (only `lender_partner` is free_text per REQUIREMENTS line 85), `ALL_TRACKS`/`TrackCode`/`RoleSlotCode`/`ROLE_SLOT_CODES` type surface, `isRoleValidForTrack(role, trackCode)` pure PEOPLE-05 primitive (returns false on unknown role OR unknown track OR cross-track misuse), and `slotsForTrack(trackCode)` UI helper. Universal slots (`directing_agent`, `main_contact`, `internal_owner`) use the `"ALL"` sentinel instead of enumerating 8 codes. Conservative `title_partner='TE only'` default documented in module header as P10 calibration candidate. `lib/contact-schema.ts` (117 lines) ships 5 `.strict()` Zod contracts: `createContactSchema` (6 fields, fullName trim+min 1+max 200, email blank-or-valid union), `updateContactSchema` (partial), `contactIdSchema` (split-parse for server actions), `upsertDealPersonSchema` ({dealId uuid, role z.enum(ROLE_SLOT_CODES), contactId nullable, freeTextValue trim+min 1+max 200 nullable}), `removeDealPersonSchema` ({dealId uuid, role}). Role enum consumed from `ROLE_SLOT_CODES` array — slot edits become one-file changes. Track-compat check stays in server-action layer (Plan 04), not Zod, so the contract remains DB-free and client-side-RHF reusable. 46 new unit assertions across 2 files (22 role-slots: ROLE_SLOTS length=12/code-set/free_text uniqueness + isRoleValidForTrack matrix + slotsForTrack membership; 24 contact-schema: happy/trim/unknown-key/email-union/partial-updates/uuid/role-enum/free-text). Full suite **228/228** (was 182 pre-plan); `npx tsc --noEmit` clean; `npm run build` clean (4 routes unchanged). Commits `443965c` (test RED role-slots) → `e899279` (feat GREEN role-slots) → `30763b2` (test RED contact-schema) → `0aface1` (feat GREEN contact-schema). Duration 3m 45s. Zero deviations. Requirements PEOPLE-02 (reconfirmed at app-code layer) + PEOPLE-05 complete. Next: Plan 03-03 (read-query layer) to close Wave 2, then Plan 03-04 (server actions TDD) consumes these schemas + `isRoleValidForTrack`. |
| 2026-04-18 | **P3 Plan 01 executed (3 commits + TDD). Wave 1 COMPLETE.** PEOPLE-01 + PEOPLE-02 shipped at the DB layer. `db/schema/app.ts` gains `contacts` table (10 cols: id/full_name/role_hint/org/email/phone/notes/created_by/created_at/updated_at; partial UNIQUE INDEX `contacts_email_unique_idx ON lower(email) WHERE email IS NOT NULL` for case-insensitive autosuggest dedupe without forcing null-email contacts to collide; btree on full_name for prefix search) and `dealPeople` table (6 cols: id/deal_id/contact_id/role/created_by/created_at; UNIQUE INDEX `deal_people_one_per_slot_idx ON (deal_id, role)` enforcing PEOPLE-02 one-slot-per-deal invariant at the DB; FK semantics deal_id CASCADE + contact_id SET NULL preserves audit trail + created_by NO ACTION). 4 type exports landed: `Contact`, `NewContact`, `DealPerson`, `NewDealPerson`. Migration 0006 generated via `npx drizzle-kit generate` (auto-tag `0006_flowery_bloodscream`) then renamed → `0006_contacts_and_deal_people.sql` with matching `_journal.json` tag edit. `npm run db:migrate` exits 0 first+second run (idempotent). Smoke-tested in psql with BEGIN/ROLLBACK: (1) two sequential `INSERT deal_people(..., role='main_contact', ...)` for same deal → second rejected with `deal_people_one_per_slot_idx` violation; (2) `email='DUPE@Test.com'` then `email='dupe@test.com'` → second rejected with `contacts_email_unique_idx` violation (case-insensitive via `lower()`); (3) two NULL-email contacts coexist (partial unique skips nulls). Schema-shape tests split into new file `tests/unit/schema-contacts.test.ts` (8 assertions covering PEOPLE-01/02; deliberately separate from `schema-shape.test.ts` to keep P1/P2/P3 slices independently readable). Full suite 182/182 green; `npx tsc --noEmit` clean; `npm run build` 4 routes. Commits `9bda3fa` (test RED — 7 failures, type errors) → `ab3984d` (feat GREEN — contacts + dealPeople schema) → `b801170` (feat — migration 0006 renamed + journal + snapshot). Duration 4m 52s. Zero deviations. Requirements PEOPLE-01 + PEOPLE-02 complete. Next: Wave 2 — Plans 03-02 (role-slot registry + Zod contracts) and 03-03 (read-query layer) run in parallel. |

---

## Accumulated Context

### Roadmap Evolution

- Phase 0.5 added: Cloudflare Access + Encryption Scaffolding (replaces Auth.js; adds NPI encryption + vendor register) — 2026-04-17
- Phase 5.5 added: Documents + R2 (inserted between P5 Email and P6 LLM; placeholder only, plan authored at execution time) — 2026-04-17 (rev 8)

---

## Key Decisions Log

| Decision | Rationale | Date |
|---|---|---|
| Auth.js → Cloudflare Access | Edge enforcement, native MFA, separate audit log, simpler app code. GLBA alignment. | 2026-04-17 |
| Cloudflare Tunnel for origin isolation | Makes "direct origin bypass" impossible — app accepts no public traffic | 2026-04-17 |
| Column-level NPI encryption via `@47ng/cloak` | GLBA Safeguards Rule coverage; portable; key rotation built-in | 2026-04-17 |
| `npi_access_log` table + wrapped `decrypt()` | Non-skippable NPI read audit, layer 2 of the 3-layer audit model | 2026-04-17 |
| R2 (not S3 / not Drive) for document storage | Zero egress fees; same vendor as Access → one DPA | 2026-04-17 |
| Subdomain: `portal.utstitle.com` | Clearest name; utstitle.com is brand anchor | 2026-04-17 |
| Gmail OAuth: per-user (not Workspace delegation) | Works without Workspace admin; per-user consent friction is acceptable | 2026-04-17 |
| Revert P0 Auth.js via forward commits | Preserves history; no force-push | 2026-04-17 |
| Tunnel runs as Railway sidecar service | Single-vendor compute; simplest private-networking path | 2026-04-17 |
| `ENCRYPTION_KEY` custody: Railway env var + 1Password backup; dual-key rotation via `@47ng/cloak` | Cloudflare Secrets (earlier pick) is Workers-only, not reachable from a Railway-hosted Next.js app. Railway env is canonical; 1Password is disaster recovery. | 2026-04-17 (rev 8) |
| Dropped Access Logpush (CFA-10) | Logpush requires Cloudflare Enterprise. Free tier provides a dashboard log view only. Layers 1 + 2 (`change_history` + `npi_access_log`) remain authoritative for GLBA. v2 backlog: daily cron polling CF API to archive to R2. | 2026-04-17 (rev 8) |
| Railway Hobby Postgres for P0.5.1 → P5 | Stays in Railway private network (matches P0.5 single-vendor decision); ~$5/mo fits $15-20 budget; weekly snapshots acceptable while deals are non-authoritative pre-P10. PII protection provided by column-level encryption + CF Access + audit log, not by Postgres tier. **Upgrade to Railway Pro when (a) Carrie's data becomes authoritative for daily operations, OR (b) written GLBA Safeguards program requires daily DB backups — whichever comes first. Expected timing: ~Phase 5.** | 2026-04-18 (P0.5.1 discuss) |
| Migrations auto-run on boot; seeds stay manual | `package.json prestart` runs `npm run db:migrate` every container boot (idempotent, auto-heals). Seeds run explicitly via `railway run npm run db:seed` because data modifications in a GLBA-scope system deserve deliberate human action + audit trail. `onConflictDoUpdate` protects against duplicates, not against bugs in seed files. | 2026-04-18 (P0.5.1 discuss) |
| Initial prod seed documented in `.planning/INITIAL_SEED_VERIFICATION.md` | One-time seed against prod is diff-verified (readback vs source seed files) and logged with date + outcome. Converts an invisible event into a traceable artifact. | 2026-04-18 (P0.5.1 discuss) |
| Phase P5.5 Documents + R2 inserted between P5 and P6 | Documents are half the stated portal scope; punting to Post-MVP means the stated goal doesn't ship. Between P5 (email) and P6 (LLM) places them after core CRM is usable but before integration/convenience phases. | 2026-04-17 (rev 8) |
| P1 schema split (D-04) + lookup tables (D-01) for tracks/stages | Creates template-extraction seam now (cheap 10-min refactor); lookup tables let Carrie add/rename stages in P10 via SQL INSERT instead of `ALTER TYPE` migrations | 2026-04-17 |
| D-04a: FK `npi_access_log.deal_id → deals.id` landed in 0003 | Closes the dangling reference from P0.5 — column existed, target table did not until P1 | 2026-04-17 |
| OPS-01 audit_log shipped in P1 (D-05) with denormalized user_email | Deal creation IS a GLBA-relevant mutation; avoids a P1→P2 audit gap. `user_email` denormalized so traceability survives user-row deletion | 2026-04-17 |
| DB-client convention locked: `@/lib/db` canonical, `@/db/client` forbidden | Prevents drift across plans 02–06 and the extracted starter template; `lib/db.ts` is the single `drizzle()` call site with `schema` pre-bound | 2026-04-17 |
| D-06 implemented: NEXT_PUBLIC_APP_NAME/DOMAIN/BRAND_COLOR env vars validated by Zod; AppHeader reads brand name from env; prototype sub-label dropped | Template-extraction seam — future starter users rebrand via a single `.env` edit. Bare-domain regex catches accidental `https://` prefixes at boot. | 2026-04-17 |
| D-02 / DEAL-02a implemented: hand-authored migration 0004 with plpgsql `next_file_no(state_code)` — lazy `deals_file_no_{YYYY}_seq`, null/empty → 'XX', zero-pad 4. TS wrapper `generateFileNo(tx, state)` typed with real `PgTransaction<NodePgQueryResultHKT, typeof schema, ExtractTablesWithRelations<typeof schema>>` | Postgres `nextval()` is atomic under concurrency (proven by 10-way `Promise.all` test); lazy sequence creation avoids pre-allocating sequences for every year. Proper `PgTransaction` type catches misuse at compile time (Plan 05's server action cannot accidentally pass a non-transaction). | 2026-04-17 |
| DEAL-01 + STAGE-01 seed data landed via `npm run db:seed` — idempotent `onConflictDoUpdate` keyed by `code`; seeder uses canonical `@/lib/db` client; stage FKs resolved at insert time via code→id map; dynamic imports after dotenv to sidestep ESM hoisting vs `@/lib/env` Zod validation | Spec tweaks flow through re-run (UPDATE in place, not no-op); seeder preserves the locked DB-client convention; code→id resolution avoids hardcoded UUIDs; 13 seed-shape tests encode DEAL-01 + STAGE-01 so drift fails CI | 2026-04-17 |
| DEAL-02 / DEAL-02a / DEAL-03 / OPS-01 shipped via `/deal/new`: `createDeal` wraps file_no + insert + audit in ONE `db.transaction`; `writeAuditLog` helper is transaction-scoped + denormalizes userEmail per D-05; `parseMoney` is the canonical $-prefix money normalizer | Structural atomicity (not advisory) means a failed audit rolls back the deal — eliminates P1→P2 OPS-01 gaps. Shared Zod `createDealSchema` re-validated server-side means client-side short-circuit is a UX sugar, not a security boundary. Split `CreateDealFormInput` / `CreateDealInput` types is the Zod v4 + RHF interop pattern every form with `.default(...)` will follow in P2+ | 2026-04-17 |
| shadcn base-nova preset ships no `form` primitive — wrote `components/ui/form.tsx` by hand mirroring shadcn/ui API against react-hook-form 7.72 | base-nova defers form composition to base-ui's `Field`; Plan 05's spec calls for RHF + Zod; manual wrapper keeps the canonical API surface for every future form in the app without fighting the preset | 2026-04-17 |
| `vitest.config.ts` → `fileParallelism: false` | Multiple test files (file-no.test.ts, create-deal-action.test.ts) manipulate the shared per-year Postgres sequence `deals_file_no_YYYY_seq`; parallel file execution invalidates OID cache. Within-file tests still run sequentially by default, so the cost is minimal | 2026-04-17 |
| VIEW-01 + VIEW-02 shipped via `app/page.tsx` Server Component + 4 client subcomponents; URL-state filters (D-07) via shared `lib/filter-params.ts` parse/serialize; Drizzle `$with` CTE for per-deal `MAX(audit_log.created_at)` (D-15) with `coalesce(..., updated_at)` fallback; `lib/format.ts` centralizes track/priority badge styling | Client-side filter state would have forked the URL-sharing story and added hydration cost; URL-state means every list-view link is shareable and bookmarkable. CTE-based aggregation scales without N+1 and typechecks end-to-end. Centralizing badge classes in `lib/format.ts` means P10 color-palette calibration is a single-file edit. | 2026-04-17 |
| Entire 16-file orphaned prototype-component graph deleted (plan listed only 2) — deals-view, deal-detail, notes-popup, email-threads, change-history, editable-currency, calendar-popup, track-priority-dropdowns, stage-dropdown, contact-hover-card, deal-details-popup, draft-email-dialog, mini-stage-bar, people-picker, stage-stepper, refresh-button | All 16 either imported from @/mock directly or transitively through @/components/deal-detail. Leaving 14 of them while deleting mock/ would have broken `npm run build` immediately. Dead code with live imports is a silent hazard; removing the graph makes the repo contents = the repo reality. | 2026-04-17 |
| base-nova Button uses `render={<Link />}` not `asChild` — convention propagated from Plan 05 to Plan 06 | base-nova wraps `@base-ui/react/button` which exposes a `render` prop (slot-style composition) in place of Radix's `asChild`. Using the platform's own API avoids fighting the preset. | 2026-04-17 |
| TASK-02 is_next invariant enforced at DB level via a PARTIAL UNIQUE INDEX (`CREATE UNIQUE INDEX ... ON tasks (deal_id) WHERE is_next = true`), not a CHECK trigger or app-level lock — Drizzle emits this correctly via `uniqueIndex(...).on(...).where(sql\`...\`)`. Server actions (Plan 03) are the happy path; the partial index is the safety net. | Postgres treats partial indexes as atomic uniqueness constraints only for matching rows, so many `is_next=false` rows per deal remain unconstrained while the invariant case (`is_next=true`) is rejected at write time. Smoke-tested: first is_next=true accepted → second rejected with unique_violation → is_next=false unconstrained. | 2026-04-18 |
| Tasks/notes FK semantics: CASCADE on `deal_id` (tasks+notes belong to the deal); SET NULL on `tasks.parent_task_id` + `tasks.advances_stage_to_id` (preserve task history if parent/stage removed); NO ACTION on user refs (`owner_user_id`, `created_by`) — users shouldn't be deletable while they own data | Deal-scoped child data should die with the deal (one statement cleans both). Task history is forensically valuable even if the parent task or target stage is deleted — hence SET NULL. User archival is deferred; keep the guard until a real use case demands migration to SET NULL. | 2026-04-18 |
| Migration 0005 rename: drizzle-kit emitted `0005_far_skullbuster.sql`; renamed to `0005_phase2_tasks_notes_kill.sql` + patched `_journal.json` tag to match (same pattern as P1 Plan 03) | Human-readable migration filenames are easier to reference from plan SUMMARYs and PRs; the auto-tag is fine for content but the filename is a public API. | 2026-04-18 |
| P2 update-style Zod schemas use `.strict()` + explicit field-list (not `.partial()` over createXxxSchema) so immutable fields (deals.trackCode/fileNo; tasks.dealId) are rejected by unknown-key check, not silently ignored | `.partial()` would carry immutable fields along as optional, silently allowing a client to attempt re-tracking a deal or re-parenting a task. Field-list + strict means the schema IS the edit-surface contract — grepping the shape answers "what can Carrie change?" definitively. | 2026-04-18 |
| updateDealSchema omits mainContactName/mainContactEmail even though columns exist (DEAL-02 free-text created by createDeal) | Editing those fields migrates to P3 when the contacts FK lands (D-03). Today's Overview edit scope is intentionally narrower than the DB column set; omitting from Zod keeps the contract honest — if a form tried to submit these fields, `.strict()` would reject, catching the drift at validation time. | 2026-04-18 |
| updateDealSchema.status enum is `['active','closed']` only — `'killed'` forced through killDealSchema | Kill is a destructive, reason-required transition (UI-SPEC kill dialog requires ≥ 3 chars). Admitting `status='killed'` to updateDeal would let clients bypass the reason requirement. Two schemas == two contracts; type-level enforcement that kill needs a reason. | 2026-04-18 |
| Split-parse pattern for composite-input Server Actions (`updateDeal` takes `{dealId, ...diff}`) — parse `dealIdSchema` first, then `updateDealSchema.safeParse(rest)`. `z.intersection` / `.and()` does NOT propagate `.strict()` unknown-key rejection because each sub-schema validates the full input independently and is tolerant of extras seen by the other. Canonical for future composite-input actions (e.g., Plan 03's updateTask). | The `.strict()` boundary is the only thing stopping clients from sending `fileNo` or `trackCode` to updateDeal; losing that silently would allow immutable-field writes that bypass DEAL-02a. Documented in `app/deal/[id]/actions/deals.ts` docstring. | 2026-04-18 |
| Audit `source` marker lives in `after_json` (JSONB), not a new column. Vocab: `manual_advance` / `manual_revert` / `manual_close` / `manual_kill` / `task_autoadvance` / `task_autoadvance_undo`. Tx-scoped helpers (`advanceStageInTx`) accept `source` as a parameter so Plan 03's auto-advance can pass `task_autoadvance`. | Stable schema + JSONB `->>'source'` extraction is cheap at Audit-tab filter time. Alternative (new column) would have required a migration + a NOT NULL backfill on every prior audit_log row. The source union includes Plan 03's `task_autoadvance_undo` value eagerly so helper signatures don't need to change when Plan 03 ships. | 2026-04-18 |
| Only top-level Server Actions call `revalidatePath`; tx-scoped helpers never revalidate. `revalidatePath('/deal/[id]', 'page')` is the correct Next.js 16 signature for dynamic routes (see `node_modules/next/dist/docs/01-app/03-api-reference/04-functions/revalidatePath.md` line 25-26). | Composing callers own their revalidation boundary — Plan 03's completeTask will revalidate ONCE after its whole tx commits, not per-inner-helper. Keeps the revalidation graph obvious from the action module. | 2026-04-18 |
| killDeal writes 4 rows in ONE tx: UPDATE deals (status/stage/killedAt/closedAt/killReason) + INSERT deal_notes (is_system=true) + 2 audit rows (deals.update + deal_notes.create). Idempotent: rejects `{ok:false}` if `deal.status === 'killed'` already. Rollback invariant proven by Test 9 using `vi.doMock('@/lib/audit')` (writeAuditLog throws → whole tx rolls back → deal stays active, no system note). | Mirrors P1 Plan 05 Test 7 rollback pattern. The idempotency check prevents duplicate system notes from double-clicks on the kill dialog. Multi-row mutation with audit guarantee is the D-05 atomicity contract as applied to a compound action. | 2026-04-18 |
| P2 Plan 03 audit breadcrumb pattern: completeTask merges `triggeringTaskId` onto the deals.update audit row AFTER advanceStageInTx writes it, via raw SQL `UPDATE audit_log SET after_json = after_json \|\| jsonb_build_object('triggeringTaskId', <id>::text) WHERE id = (SELECT id FROM audit_log WHERE table_name='deals' AND record_id=<dealId> AND after_json->>'source'='task_autoadvance' ORDER BY created_at DESC LIMIT 1)`. undoCompleteTask reads it back via `afterJson->>'triggeringTaskId' = taskId` to derive the revert-target stage id from `beforeJson.stage_id`. | Keeps the stages.ts helper signature stable (advanceStageInTx doesn't need to know about Plan 03's undo contract). Alternative (extend the helper to accept an optional metadata bag) would have required modifying the Plan 04 source + re-running Plan 04 tests. JSONB merge is cheap + honest — "who caused this stage advance?" is caller concern, stored alongside the stage transition it describes. | 2026-04-18 |
| P2 Plan 03 audit-lookup revert safeguard: undoCompleteTask runs the SELECT for the task_autoadvance audit row BEFORE mutating the task row. On missing audit (race/tamper), throws inside db.transaction → whole tx rolls back → task.status stays 'done', deal.stage_id unchanged. Proven by Test 8c which simulates via direct `DELETE FROM audit_log WHERE ... after_json->>'source' = 'task_autoadvance'`. | The abort must leave a consistent state. If task update ran before the audit-lookup, an abort would leave task='open' + deal unchanged — a partial revert. Running the SELECT first + throwing inside the tx makes the safeguard atomic: either the whole undo lands or nothing does. | 2026-04-18 |
| P2 Plan 03 is_next invariant proven end-to-end: 10-way `Promise.allSettled(createTask({isNext:true}))` on the same deal → exactly 1 is_next=true row survives. Mixed outcomes expected: some callers clear each other's is_next atomically (the clear-prior step in createTask); others abort on Postgres 23505 (partial unique index `tasks_one_is_next_per_deal_idx`) and return `{ok:false, error:'conflict'}`. | Concurrency tests prove the app-level happy path (clear-before-insert inside one tx) AND the DB-level backstop (partial unique index) both work and compose correctly. Neither alone is sufficient: without the app-level clear, every createTask({isNext:true}) after the first would fail; without the index, a tx-interleave race could leak two is_next rows. | 2026-04-18 |
| P2 Plan 03 Result type: 3-way discriminated union `{ok:true, ...} \| {ok:false, errors:Record<string,string[]>} \| {ok:false, error:string}` — Zod validation errors stay separate from runtime errors (conflict, DB failure). Tests narrow via `"errors" in result` to select the validation branch. | Downstream UI code (react-hook-form) wants field-level error messages from `result.errors.title[0]`. Runtime errors show a destructive toast instead. Collapsing the two into `{ok:false, errors}` would force the UI to special-case a `{_runtime: [...]}` key; separating keeps each failure mode self-describing. | 2026-04-18 |
| P2 Plan 03 queryTasksForDeal: ONE SELECT with `leftJoin(users)` returns every task for the deal + owner_name; in-memory split into `{open, done}`. Open ordered by `is_next DESC, due_date ASC NULLS LAST, created_at ASC`; done re-sorted in JS by `completedAt DESC`. | The Tasks tab renders the open list and the collapsed done list on the same page load. Two separate queries would double the round-trip; splitting in-memory costs one pass over the result array (negligible at Carrie's ~50 open deals × ~5 tasks per deal scale). | 2026-04-18 |
| P2 Plan 03 reassignTask as updateTask wrapper: `export async function reassignTask(id, ownerUserId) { return updateTask({id, ownerUserId}); }` — one line, one import for Plan 06's owner popover. No duplicated tx/audit logic. | Plan 06's UI wants a semantically-named import (`reassignTask(id, newOwnerId)`), not `updateTask({id, ownerUserId: newOwnerId})`. A wrapper keeps the export surface descriptive without code duplication. | 2026-04-18 |
| P0.5.1 Plan 01: prestart npm-lifecycle hook lives in package.json (NOT railway.json postdeploy). Migrations auto-apply on every container boot; seeds stay manual. DEPLOY.md records prestart-only migration policy — operators do NOT run db:migrate from a laptop. | Platform-agnostic (survives compute-vendor swap); fails safe (Railway keeps old deploy if prestart fails); GLBA-aligned (data modifications stay deliberate-human-action). Confirmed locally: empty DB → drizzle.__drizzle_migrations COUNT=6 after prestart fire. | 2026-04-18 (P0.5.1-01) |
| P0.5.1 Plan 01: `.planning/DEPLOY.md` is the LIVING ongoing production runbook for every future phase. 6 sections locked: push-to-prod playbook / env-var inventory / 3-layer rollback / initial-seed checklist / CFA-11+12 closure verifications / compliance notes. Layer 3 (forward-compensating migration) acknowledges drizzle-kit doesn't auto-generate down migrations. | Runbook-as-planning-artifact pattern: `.planning/DEPLOY.md` evolves across phases (update at every phase transition where deploy posture changes) rather than being snapshot-shipped. Keeps operator reference in the same tree as plans + state. | 2026-04-18 (P0.5.1-01) |
| P0.5.1 Plan 01: `.planning/INITIAL_SEED_VERIFICATION.md` converts invisible one-time prod seed into a dated + operator-stamped + commit-SHA-stamped traceable artifact. Fields: run metadata, row counts (actual vs expected 8 tracks / 25 stages / 4 universal / 10 TE / 11 FL), field-level diff, outcome enum (verified / mismatch-resolved / mismatch-escalated). Readback via `node -e` + `pg` v8.20.0 — no `psql` fallback. | GLBA Safeguards: initial seed IS a data modification event; must be auditable after the fact. `pg` is a dep already; `psql` not guaranteed in app container. Template created blank in Plan 01, filled in Plan 03. | 2026-04-18 (P0.5.1-01) |
| P3 Plan 02: PEOPLE-02 role-slot taxonomy encoded as typed readonly `ROLE_SLOTS` data in `lib/role-slots.ts` (12 entries with `{code, label, appliesToTracks, source, notes?}`). `source: "contact_fk" \| "free_text"` — only `lender_partner` is free_text per REQUIREMENTS.md line 85 ("actual lender on the deal, distinct from the mortgage partner; often not in registry"). Universal slots (`directing_agent`/`main_contact`/`internal_owner`) use `appliesToTracks: "ALL"` sentinel. | Single source of truth. P10 calibration week edits are a one-file change. Server actions (P3-04) and UI (P3-05/06) import the same module — no taxonomy drift possible. Sentinel avoids enumerating 8 tracks for universal slots and stays trivially extendable if a 9th track lands. | 2026-04-18 (P3-02) |
| P3 Plan 02: Conservative `title_partner='TE only'` default (not TE+FL). | REQUIREMENTS PEOPLE-02 note "(autosuggest from partner contacts)" sits in a Title-and-Escrow context. Shipping the narrow default + a module-header note flagging this as a P10 calibration candidate is safer than shipping broad and narrowing later. If Carrie pushes back during calibration week it's a one-line edit to the `appliesToTracks` array. | 2026-04-18 (P3-02) |
| P3 Plan 02: PEOPLE-05 enforcement split in TWO layers. Layer 1 (`isRoleValidForTrack(role, trackCode)` in `lib/role-slots.ts`) is pure: returns false on unknown role OR unknown track OR cross-track misuse. Layer 2 (Zod schemas in `lib/contact-schema.ts`) consumes the role enum from `ROLE_SLOT_CODES` so role membership is validated at the contract boundary, but track compatibility is deferred to the server action (Plan 04) because the deal's track code is only known after loading the deal row. | Keeps `lib/contact-schema.ts` DB-free and reusable for client-side RHF validation. Zod validates shape + role membership (what a client could know); server action loads deal, calls `isRoleValidForTrack`, and surfaces the track-compat error as a field-level message. Splitting the layers mirrors P2's strict schemas + server-action-owned cross-field checks pattern. | 2026-04-18 (P3-02) |
| P3 Plan 02: Role enum in 4 Zod schemas consumed from `ROLE_SLOT_CODES` array (cast to `[string, ...string[]]` for `z.enum`) — NOT hardcoded as a z.enum literal. | Adding/removing/renaming a slot is a single edit to `lib/role-slots.ts` — Zod picks up the change automatically, and TS type-checks all consumers of `RoleSlotCode`. Hardcoded enums in schema files would drift; the array cast is a tiny ergonomic cost vs. taxonomy-coherence safety. | 2026-04-18 (P3-02) |
| P3 Plan 02: `lender_partner` free_text storage strategy — Plan 04 server action will synthesize a minimal contact row (`full_name=<text>, role_hint='lender', org=<text>, no email`) on upsert rather than introducing a sidecar free-text column on `deal_people`. | Keeps the `deal_people` shape uniform (FK-to-contacts semantics for all 12 slots). Alternative (sidecar `free_text_value` column) would have required a migration + conditional read logic at every query. Synthesizing a contact row preserves the autosuggest-dedupe benefits of the existing `contacts_email_unique_idx` for lenders who DO eventually enter an email. | 2026-04-18 (P3-02) |
| P3 Plan 02: Split-parse `contactIdSchema` mirrors P2 `dealIdSchema` — Plan 04's `updateContact` server action will parse `{id}` first and then `updateContactSchema.safeParse(rest)`. `.and()` composition does NOT propagate `.strict()` (each sub-schema is tolerant of the other's fields), so the id+body pair must be two separate `.strict()` objects. | Canonical pattern already established in P2 for `updateDeal`. Applying it consistently across P3 means every composite-input mutation action uses the same shape. The `.strict()` boundary is the only thing rejecting spurious keys (e.g., `contactId: <other-uuid>` passed by a misbehaving client trying to reparent a contact); losing it silently would be a correctness bug. | 2026-04-18 (P3-02) |

---

## Next Action

**Phase 0.5.1 in flight — Plan 01 of 3 complete (DEPLOY-09 shipped + runbook scaffolded).** Plan 02 unblocked.

**Delivered by P0.5.1 Plan 01 (just completed):**

- `package.json` `"prestart": "npm run db:migrate"` — DEPLOY-09 closed. npm lifecycle fires `prestart` automatically before `start`, so Railway container boot will auto-apply migrations before Next.js serves. Locked per CONTEXT §Migration Hook Style (platform-agnostic; fails-safe).
- `.planning/DEPLOY.md` — 6-section ongoing runbook (push playbook / env inventory / 3-layer rollback / initial-seed checklist / CFA-11+12 closure verifications / compliance notes). Prestart-only migration policy recorded in §1 + §4.
- `.planning/INITIAL_SEED_VERIFICATION.md` — template with run metadata + row counts + field diff sections + outcome enum, ready for Plan 03 to fill after the one-time prod seed.
- Zero prod contact — all verification local (`docker compose` Postgres + `npm start` full-chain capture + 170/170 tests + tsc + build). Empty-DB readback: `SELECT COUNT(*) FROM drizzle.__drizzle_migrations` = 6.
- Brand-color default confirmed: `.env.local` + `.env.example` both have `NEXT_PUBLIC_APP_BRAND_COLOR=` empty; `#0F172A` will be pinned deterministically by Plan 02 Task 3.
- Commits: `41ecb85` (feat prestart) → `10c5817` (docs runbook + template). Duration 15m 35s. Zero deviations.

**Next:** `/gsd:execute-phase 00.5.1` → **Plan 02** (Railway Postgres provision + wire `DATABASE_URL=${{Postgres.DATABASE_URL}}` reference var + set `NEXT_PUBLIC_APP_NAME` / `_DOMAIN` / `_BRAND_COLOR=#0F172A` / any missing env → trigger first real prod deploy; prestart will auto-apply all 6 migrations against fresh prod Postgres on first boot). Plan 02 is the first plan to touch Railway.

**After Plan 02:** Plan 03 — one-time `railway run -- npm run db:seed` + fill in `INITIAL_SEED_VERIFICATION.md` + execute CFA-11 (direct-origin 403/404 curl) + CFA-12 (avatar shows real user after CF Access login) closure verifications.

---

## Historical Next Action

**Phase 2 COMPLETE — ready for Phase 3 transition.** All 6 P2 plans shipped; all 5 Phase 2 success criteria proven end-to-end.

**Delivered by P2 Plan 06 (final Phase 2 plan):**

- Three tabs wired from scratch: Tasks, Notes, Audit — replacing the three placeholder panels in `deal-tabs.tsx` from Plan 05.
- **Tasks tab:** OPEN section (is_next ordered first per queryTasksForDeal) + collapsible DONE section (default collapsed when count ≥ 5) + empty state with ListChecks icon + primary CTA. Each open task row shows Checkbox + `border-l-2 border-primary pl-3` accent bar for is_next=true (the P2-NEW accent per UI-SPEC line 130) + `◀ Next` chip + Body-tier title + relative-time due (overdue→destructive, today→amber, future→muted) + `@owner`. Checking fires 5-second toast with Undo button; undo payload includes autoPromotedTaskId + priorIsNext.
- **W2/W3 contract compliance:** task-row.tsx uses `result.stageAdvanced.toLabel` (NOT toCode, NOT to) and `result.newIsNext.title` verbatim in toast copy. undoCompleteTask receives `autoPromotedTaskId: result.newIsNext?.id ?? null`.
- **New Task dialog:** 6 fields (title required / owner Select / due date Popover+Calendar / parent task / advances_stage_to / is_next Checkbox) via RHF+zodResolver(createTaskSchema). Reachable-stages filter excludes `killed` and non-track-compatible stages.
- **Notes tab:** Textarea composer with "Add a note…" placeholder; `Save note` CTA only visible when body has content. `createNote` Server Action wraps INSERT + writeAuditLog in ONE db.transaction per D-05. Rollback invariant proven by Test 3. Newest-first Card list with Avatar initials + relativeTime + `system` chip for is_system=true rows.
- **Audit tab:** Sticky filter header with 5 chips (All/Deal/Tasks/Notes/Stage) as shadcn outline Buttons; active chip gets `bg-primary text-primary-foreground`. URL-state via `?auditFilter=<key>` with tolerant parseAuditFilter. Rows render reverse-chron with mono meta line + diff lines. Arrow glyph `→`, italic muted `null`, money/date formatters, operation-awareness (create=after-only, delete=strikethrough, update=arrow). IGNORE_KEYS suppresses audit-internal noise (`source`, `triggeringTaskId`).
- **Server-side audit filter SQL:** deal/tasks/notes/stage filter via Drizzle on audit_log with subqueries for task/note id scoping. Stage filter uses `IS DISTINCT FROM` on `beforeJson->>'stage_id'` vs `afterJson->>'stage_id'` for explicit NULL handling.
- **page.tsx Promise.all:** 4 parallel readers (queryTasksForDeal + listUsers + queryNotesForDeal + queryAuditForDeal).
- 3 new lib/ query modules + 1 new action module + 4 new client components + 1 new test file. Full suite 166 → 170 green (+4 note-action tests); tsc + build clean (4 routes).
- **Phase 2 success criterion 5 proven:** every mutation from Plans 03/04/05/06 now appears in the Audit tab with full before/after — deals.update (Overview edits), deals.update with source markers (stage advance/revert, close, kill), tasks.create/update (task mutations), deal_notes.create (notes + kill system notes).

**Phase 2 success criteria — all 5 met end-to-end:**

1. ✅ `/deal/[id]` renders with tabs; Overview editable; Notes, Audit visible (Plans 05 + 06)
2. ✅ Tasks maintain exactly-one is_next invariant (Plan 03 partial unique index + app-layer clear-prior; 10-way concurrency test proves)
3. ✅ Stage advance requires two-click confirm, writes audit_log, updates list view (Plans 04 + 05 dialogs)
4. ✅ Marking deal killed requires reason note; writes audit_log (Plan 04 killDeal + Plan 05 KillDealDialog with ≥3-char reason)
5. ✅ Every mutation appears in Audit tab with before/after (Plan 06 Audit tab over Plans 03/04/05 audit_log writes)

**Next:** Phase transition to **Phase 3 (People Map & Contacts Registry)**. P3 will rename the File Contacts tab from placeholder to real content, migrate free-text `main_contact_*` columns to a contacts FK (D-03), add per-deal role slot assignments, and stand up the `/contacts` global list.

**Outstanding `Ship + revisit` UI-SPEC calls for Carrie UAT (from 02-UI-SPEC.md open questions):** Assumptions 1 (per-card Overview edit), 2 (overflow menu for kill/close), 4 (append-only notes), 7 (auto-advance skips confirm), 9 (no click-to-edit on individual Overview fields), 11 (accent color extension), 12 (no dirty-form confirmation).

To execute: `/clear` (fresh context), then `/gsd:discuss-phase 3` or `/gsd:ui-phase 3` → `/gsd:plan-phase 3` → `/gsd:execute-phase 3`.

**Historical plans:**

**Delivered by P2 Plan 03:**

- `app/deal/[id]/actions/tasks.ts` — 5 Server Actions: `createTask`, `completeTask`, `undoCompleteTask`, `updateTask`, `reassignTask`. All wrap DML + audit in ONE `db.transaction` per D-05. completeTask composes `advanceStageInTx` inline for TASK-01 auto-advance and merges `triggeringTaskId` onto the deals.update audit afterJson. undoCompleteTask does audit-lookup revert via `revertStageInTx` and aborts with `Cannot safely revert stage` (tx rollback) when the breadcrumb audit row is missing.
- `lib/tasks-query.ts` — `queryTasksForDeal(dealId)` reader: one SELECT with leftJoin(users), ORDER BY is_next DESC + due_date ASC NULLS LAST + created_at ASC; in-memory split into `{open, done}`; done re-sorted by completedAt DESC. Ready for Plan 05/06 consumption.
- TASK-02 is_next invariant proven end-to-end by 10-way `Promise.allSettled` concurrency test — exactly 1 is_next=true row survives (partial unique index + app-level clear-before-insert compose correctly).
- Audit source marker vocab widened with `user_action` (create/update), `task_undo_complete` (undo flip). `task_autoadvance` / `task_autoadvance_undo` sources already admitted by Plan 04 union.
- 13 new real-Postgres tests; full suite 153 → 166 green; tsc + build clean
- Requirements TASK-01, TASK-02, TASK-03, TASK-04, TASK-05 complete

**Next:** `/gsd:execute-phase 2` → **Plan 05** (deal detail shell + header + stepper + Overview tab + 4 mutation dialogs) OR **Plan 06** (Tasks + Notes + Audit tabs). Both are wave 3, neither blocks on the other — can run in parallel if supported, or pick either order.

**Unblocked downstream P2 plans:**

- 05: deal detail shell + Overview edit (consumes `updateDeal` from Plan 04, `queryDealById` TBD in Plan 05, Overview copy from UI-SPEC)
- 06: Tasks + Notes + Audit tabs (consumes `queryTasksForDeal` + `completeTask` + `undoCompleteTask` + `createTask` + `updateTask` + `reassignTask` from this plan, `createNoteSchema` from Plan 02, audit_log filtering on source marker vocab from Plans 03 + 04)

---

## Historical Next Action

Begin Phase 0.5 — 14 tasks across 3 stages in `.planning/phases/00.5-access-encryption/PLAN.md`.

**Stage A — Revert Auth.js (no prereqs):**

1. Task A1: `npm uninstall next-auth @auth/drizzle-adapter`
2. Task A2: Delete Auth.js files + temporarily no-op `proxy.ts`
3. Task A3: Drop Auth.js tables from schema

**Stage B — Cloudflare Access integration (needs user tasks B1 + B2 first):**

4. Task B1 `[USER]` — Zero Trust + Access app + Google IdP + MFA + policy
5. Task B2 `[USER]` — Cloudflare Tunnel token
6. Task B3: Deploy `cloudflared` sidecar to Railway
7. Task B4: `lib/access.ts` — JWT verification (TDD)
8. Task B5: `lib/users.ts` — find/upsert by email
9. Task B6: Rewrite `proxy.ts` — enforce Access JWT
10. Task B7: Rewrite `app-header.tsx` — read Access claims

**Stage C — Encryption + NPI audit (Task C2 is user):**

11. Task C1: Add `npi_access_log` table to schema
12. Task C2 `[USER]` — Generate + store `ENCRYPTION_KEY` in CF Secrets + Railway + 1Password
13. Task C3: `lib/crypto.ts` with non-skippable audit invariant (TDD)
14. Task C4: `.planning/VENDORS.md` scaffold

To execute: `/clear` (fresh context), then `/gsd:execute-phase 0.5` or run tasks manually.
